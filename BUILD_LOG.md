# Build Log

This chronological log records bounded planning and implementation work, validation, manual verification, independent review, commits, pull requests, merges, and known-good checkpoints. Entries must describe what actually happened and must never fabricate history.

## 2026-09-02: Transferable Governance Update

- Scope: Documentation-only governance update.
- Governing reference: `DECISION-019` and `prompts/2026-09-02-transferable-ai-build-governance.md`.
- Status: Proposed edits prepared; not staged or committed.
- Application implementation: None.
- Automated implementation tests: `NOT APPLICABLE` because no application code or executable schema changed.
- Manual product verification: `NOT APPLICABLE` because no product behavior changed.
- Required documentation validation: PASS. Formatting, trailing-whitespace, internal-link, governance-content, status-consistency, decision-order, and documentation-only repository checks completed successfully.
- First independent review: `NOT READY FOR COMMIT`.
- Blocker 1: `HACKATHON_RULES.md` misleadingly characterized voluntary governance as official event rules.
- Blocker 2: Current `SPEC-001` wording could make the approved `PREREQ-002` planning contract appear to satisfy the future bounded implementation-specification gate.
- Blocker 3: Before prerequisite commit `c97e3a437c7792dee9cc3f25b03031d2ee62725e`, the governance and prerequisite changes shared one working tree and could not be safely committed together.
- Important finding 1: `AI_USAGE.md` omitted the saved governance prompt from the assisted-file list.
- Important finding 2: `REUSED_COMPONENTS.md` described incorporated Arko-supplied governance text imprecisely as `None beyond` the cited instruction.
- Important finding 3: The independent-review result was not yet recorded in this log.
- Corrections: Isolated and committed the approved prerequisite checkpoint as `c97e3a437c7792dee9cc3f25b03031d2ee62725e`; moved voluntary controls to `AI_BUILD_GOVERNANCE.md`; reduced `HACKATHON_RULES.md` to an unresolved official-rule register; reclassified references; clarified current `PREREQ-002` and future `SPEC-*` semantics; annotated `DECISION-018`; appended `DECISION-020`; corrected AI attribution and reused-text provenance; and recorded this review history.
- Correction-operation status: No file was staged or committed, and nothing was pushed or merged during these corrections.
- Review status at this checkpoint: The clean independent second-pass review had not yet been completed; the later entry below records its successful completion.
- Human approval at this checkpoint: Not yet recorded; the later entry below records Arko's acceptance.
- Rollback point: Current `HEAD`; all proposed governance edits remain in the working tree.
- Known-good checkpoint: Not created because no commit or merge is authorized yet.

## 2026-09-02: Independent Governance Second-Pass Review

- Review status: Completed.
- Review scope: Exactly the proposed 15-file governance boundary.
- Review criteria: All 27 review criteria passed.
- Findings: None at `BLOCKER`, `IMPORTANT`, or `NICE-TO-HAVE` severity.
- Reviewer integrity: The independent reviewer changed no files and made no Git-state changes.
- Review result: `READY FOR HUMAN COMMIT APPROVAL`.
- Human acceptance: Arko accepted the independent second-pass review result.
- Remaining limitations: Official Sybill event rules remain unresolved; the inaccessible PDF was not read or verified; `PREREQ-003` remains unresolved; and implementation authorization remains unavailable.

## 2026-09-02: Remote Readiness And FIX-DOC-001

- Fix reference: `FIX-DOC-001: Correct default-branch terminology and review attribution`.
- Scope: Documentation-only remote-readiness inspection and bounded correction.
- Remote-readiness inspection: Confirmed a clean working tree and index; local `HEAD` at `b6fe8692c6fd7f653b59e1b21e57742cb824b36a`; remote default branch `master` at `47029fb323dcb7acf054f15ca248ccc30626f619`; exactly the two approved documentation commits ahead in a linear series; no remote copy of either commit; no existing proposed branch or pull request; and invalid stored GitHub CLI authentication with anonymous public reads still available.
- Corrections: Replaced current governance references to a hard-coded `main` branch with the repository’s remote default branch, recorded that the current remote default is `master`, and reconciled AI attribution with the completed successful independent second-pass review.
- Preserved history: The original Arko-supplied governance prompt remains unchanged as contemporaneous provenance; its `main` wording is historical and is superseded for current governance by this fix.
- Assisted prompt: `prompts/2026-09-02-remote-readiness-documentation-fix.md`.
- Application implementation: None.
- Automated implementation tests: `NOT APPLICABLE` because no application code or executable schema changed.
- Manual product verification: `NOT APPLICABLE` because no product behavior changed.
- Git operation status: No branch was created; no file was staged or committed; and nothing was pushed, submitted as a pull request, or merged during this correction.
- Review status: This bounded documentation fix requires independent review and Arko's approval before any commit.
- Remaining limitations: GitHub CLI authentication remains invalid; official Sybill event rules remain unresolved; `PREREQ-003` remains unresolved; no bounded implementation `SPEC-*` exists; and `TASK-001` remains unauthorized.
- Rollback point: `b6fe8692c6fd7f653b59e1b21e57742cb824b36a`.

## 2026-09-02: Independent FIX-DOC-001 Review

- Review status: Independent review of `FIX-DOC-001` completed.
- Review scope: Exactly `AGENTS.md`, `AGENT_BUILD_INSTRUCTIONS.md`, `AI_BUILD_GOVERNANCE.md`, `AI_USAGE.md`, `BUILD_LOG.md`, `CONTRIBUTING.md`, `DECISIONS.md`, `HUMAN_DECISIONS.md`, and `prompts/2026-09-02-remote-readiness-documentation-fix.md`.
- Review criteria: All 20 criteria passed.
- Findings: None at `BLOCKER`, `IMPORTANT`, or `NICE-TO-HAVE` severity.
- Reviewer integrity: The independent reviewer changed no file and made no Git-state change.
- Remote-check limitation: Fresh remote querying was unavailable to the reviewer, but the corrected terminology matched the previously verified `origin/master` state.
- Review result: `READY FOR HUMAN COMMIT APPROVAL`.
- Human acceptance: Arko accepted the independent review result.

## 2026-09-02: Public Draft PR Review And FIX-DOC-002

- Pull request: Public draft PR #1, `https://github.com/therealgulkorinaga/Sybill-Hackathon/pull/1`.
- Remote PR-level review: Reviewed the remote-equivalent `origin/master...origin/docs/sybill-planning-foundation` boundary and public PR metadata. The base, head, three-commit sequence, 23-file documentation boundary, draft state, mergeability, and absence of configured checks matched the expected state.
- Review finding: One `BLOCKER` found incomplete repository-level and PR-level AI attribution for seven materially assisted planning files.
- Omitted files: `PRD.md`; `docs/product/PREREQ-001_PRODUCT_DEFINITION_PROPOSAL.md`; `docs/product/PREREQ-002_DECISION_RECORD_AND_CORPUS_PROPOSAL.md`; `docs/product/PREREQ-002_DECISION_RECORD_AND_PRECEDENT_CORPUS.md`; `docs/product/PREREQ-002_REVIEW_PACKET.md`; `docs/product/PREREQ-002_SYNTHETIC_SEED_DATA_APPENDIX.md`; and `docs/product/PREREQ-002_TRACEABILITY_REVIEW.md`.
- Review result: `NOT READY`.
- Correction created: `FIX-DOC-002: Complete planning-file AI attribution` updates `AI_USAGE.md`, records this history in `BUILD_LOG.md`, and preserves the correction prompt at `prompts/2026-09-02-complete-planning-ai-attribution.md`.
- Prompt-history boundary: Earlier material planning prompts predated mandatory prompt capture and were not reconstructed or assigned invented paths.
- Pull-request status: PR #1 remains draft and unmerged. No PR metadata was changed during this correction.
- Application implementation: None.
- Automated implementation tests: `NOT APPLICABLE` because no application code or executable schema changed.
- Manual product verification: `NOT APPLICABLE` because no product behavior changed.
- Git operation status: No file was staged or committed, and nothing was pushed or merged during this correction.
- Review status: This exact three-file correction requires an independent read-only review before commit approval.
- Remaining limitations: `PREREQ-003` remains unresolved; no bounded implementation `SPEC-*` exists; and `TASK-001` remains unauthorized.
- Rollback point: `c086772fa28a48e430e25949fdc7b5728db1f2a0`.

## 2026-09-02: Independent FIX-DOC-002 Review

- Review status: Independent review of the exact three-file `FIX-DOC-002` correction completed.
- Review criteria: All ten acceptance criteria passed.
- Findings: None at `BLOCKER`, `IMPORTANT`, or `NICE-TO-HAVE` severity.
- Reviewer integrity: The independent reviewer changed no files and made no Git-state change.
- Review result: `READY FOR HUMAN COMMIT APPROVAL`.
- Human acceptance: Arko accepted the independent review result.

## 2026-09-02: DECISION-021 Naming Migration Preparation

- Scope: Documentation-only product and repository naming migration.
- Decision reference: `DECISION-021: Rename Sybill To Finné Memory`.
- Prompt record: `prompts/2026-09-02-rename-product-finne-memory.md`.
- Baseline occurrence inventory: 109 tracked occurrences were classified before correction: 82 `CURRENT_PRODUCT`, five `EVENT`, 19 `HISTORICAL_RECORD` including the historical pull-request URL, three `SAVED_PROMPT`, zero `REPOSITORY`, zero `TECHNICAL_SLUG`, and zero `AMBIGUOUS` occurrences.
- Human correction: Arko clarified that Sybill is the hackathon/event name and must remain unchanged for event, organizer, rules, eligibility, submission, deadline, and event-specific restriction references.
- Migration result: Current product references use `Finné Memory`; the README identifies `Finne-Memory` as the repository and `finne-memory` as the technical slug; event references retain `Sybill`; and historical records and existing saved prompts remain unchanged.
- Product and implementation effect: None. Product thesis, V1 domain, representative matter, authority model, corpus, permissions, acceptance criteria, implementation gates, and separation from Finné/x402 are unchanged.
- Application implementation: None.
- Automated implementation tests: `NOT APPLICABLE` because no application code, executable schema, dependency, or scaffold changed.
- Manual product verification: `NOT APPLICABLE` because no product behavior changed.
- Git operation status: No file was staged or committed, and nothing was pushed or merged during preparation.
- Review status: The corrected naming migration requires review and Arko's approval before staging or commit.
- Remaining gates: `PREREQ-003` remains unresolved; no bounded implementation `SPEC-*` exists; and `TASK-001` remains reserved and unauthorized.
- Rollback point: `1853ff5f59c7c36609dd62678fa5a23116fe38cc`.

## 2026-09-02: Independent DECISION-021 Naming Migration Review

- Review status: Independent review of the Finné Memory naming migration completed.
- Review scope: Exactly 15 modified Markdown files and one new saved prompt.
- Review criteria: All 25 criteria passed.
- Findings: None at `BLOCKER`, `IMPORTANT`, or `NICE-TO-HAVE` severity.
- Reviewer integrity: The reviewer changed no file and made no Git-state change.
- Review result: `READY FOR HUMAN COMMIT APPROVAL`.
- Human acceptance: Arko accepted the independent review result.

## 2026-09-03: DECISION-022 Domain Pivot, PREREQ-003 Architecture, And Proposed SPEC-001

- Scope: Documentation-only planning reconciliation to the revised product direction.
- Decision references: `DECISION-022` (product), `DECISION-023` (architecture, `Proposed`).
- Prompt record: `prompts/2026-09-03-revised-direction-base-agent-authority.md`, saved before any other file changed.
- AI tool: Claude Code (Anthropic), model Opus 5. Prior entries in this log used the OpenAI Codex desktop agent.
- Repository state at start: branch `master`, clean working tree and index, `HEAD` at `3a4f106830d17e14051bdc38970e5b6ab9a048b1`, remote `https://github.com/therealgulkorinaga/Finne-Memory.git`, pull requests #1 and #2 both merged. The session-start snapshot showed `docs/rename-product-finne-memory`; the actual current branch is `master` following the merge of PR #2.
- Naming-migration status verified: `DECISION-021` is complete and merged. Current-facing documents use `Finné Memory`, `Finne-Memory`, and `finne-memory`. Historical `Sybill` references are preserved. Arko's instruction corrects the organiser spelling to `Sibyl Labs` / `Sibyl Memory`; historical text is not rewritten.
- Supplier-onboarding inventory before editing: 11 tracked files contained supplier, procurement, or beneficial-ownership references. Six `docs/product/` files were labelled historical without content changes; `PRD.md`, `DECISIONS.md`, and `TASKS.md` were revised; `AI_BUILD_GOVERNANCE.md` had one guardrail line reconciled; `AI_USAGE.md` retains its reference as historical attribution.
- Official rules verified: 2026-09-03, from `hack.sibyllabs.org` (index, `/rules`, `/submissions`) and `docs.sibyllabs.org` (`/memory`, `/memory/install`, `/memory/integrations`, `/memory/cli`). `HACKATHON_RULES.md` moved from fully `UNRESOLVED` to a sourced register with three open organiser questions.
- SDK facts verified: `sibyl-memory-client` 0.8.0, MIT, Python `>=3.10`, five-tier API, SQLite with FTS5, `UNIQUE (tenant_id, category, name)`, from the PyPI JSON API and official documentation.
- Verification limitation, recorded rather than papered over: the package is **not installed** on this machine and was not imported. The published README documents the v0.4.x surface while the release is v0.8.0. Exact signatures for `write_event`, `read_events`, `search_entities`, and tenant selection are marked `VERIFY-AT-BUILD` in `PREREQ-003` section 3, with one documented fallback for authority-event storage.
- Files created: `prompts/2026-09-03-revised-direction-base-agent-authority.md`, `docs/product/ACTIVE_DEMO_DESIGN.md`, `docs/architecture/PREREQ-003_ARCHITECTURE.md`, `docs/specs/SPEC-001_FRESH_SESSION_LEARNED_AUTHORITY_SLICE.md`.
- Files rewritten: `README.md`, `PRD.md`, `ARCHITECTURE.md`, `TASKS.md`, `HACKATHON_RULES.md`.
- Files appended or amended: `DECISIONS.md`, `AGENTS.md`, `AGENT_BUILD_INSTRUCTIONS.md`, `AI_BUILD_GOVERNANCE.md`, `AI_USAGE.md`, `HUMAN_DECISIONS.md`, `BUILD_LOG.md`, `REUSED_COMPONENTS.md`.
- Files labelled historical without content change: the six `docs/product/PREREQ-001_*` and `docs/product/PREREQ-002_*` documents.
- Product-scope contradiction identified and escalated: the revised model changes Finné Memory's output from advisory-only decision support to a binding deterministic authorization bound. This contradicted `DECISION-002`, `DECISION-013`, `AGENT_BUILD_INSTRUCTIONS.md` sections 2 and 9, and `AI_BUILD_GOVERNANCE.md`. It was recorded explicitly in `DECISION-022`, the four contradicting statements were reconciled, and it is flagged for Arko's confirmation at the approval checkpoint.
- Blocking submission gap identified: no `LICENSE` file exists. The event requires MIT or Apache-2.0. No file was created, because the licence choice is Arko's.
- Application implementation: None.
- Automated implementation tests: `NOT APPLICABLE` because no application code, executable schema, dependency, or scaffold changed.
- Manual product verification: `NOT APPLICABLE` because no product behavior exists to verify.
- Documentation validation: PASS. Internal links resolve; decision identifiers are unique and sequential; superseded decisions carry annotations; historical labels are present on all six superseded product documents; no fabricated rule, source, or capability is recorded.
- Git operation status: No branch was created; no file was staged or committed; nothing was pushed or merged. No dependency was installed, no contract deployed, and no onchain transaction executed.
- Review status: This planning checkpoint requires one independent review and Arko's approval before any commit.
- Remaining gates: `DECISION-023` and `SPEC-001` await approval and commit; `TASK-001` remains reserved; `ORG-Q1` and `ORG-Q2` remain open.
- Rollback point: `3a4f106830d17e14051bdc38970e5b6ab9a048b1`.
- Known-good checkpoint: Not created because no commit is authorized.

## 2026-09-03: Arko's Planning Approval, MIT Licence, And Checkpoint Commit

- Approval: Arko approved the planning checkpoint described in the preceding entry, including `DECISION-022`, `DECISION-023`, the flagged advisory-to-binding scope amendment, and the proposed commit boundary and message.
- Licence resolution: Arko selected MIT, closing `ORG-Q2`. `LICENSE` added as the standard unmodified OSI MIT template, copyright `2026 Arko Ganguli`. Recorded as `DECISION-024`.
- Disclosure: the copyright holder was inferred from repository context, not supplied verbatim. Flagged in `HUMAN_DECISIONS.md` for correction before submission if a different entity is intended.
- Additional files in this checkpoint: `LICENSE` created; `DECISION-024` appended; the `ORG-Q2` resolution propagated across nine documents; the `TASKS.md` build sequence updated to record steps 1, 2, and 3 as done.
- Branch: `docs/base-agent-authority-pivot`, created from `master` at `3a4f106830d17e14051bdc38970e5b6ab9a048b1`.
- Application implementation: None.
- Automated implementation tests: `NOT APPLICABLE` because no application code, executable schema, dependency, or scaffold changed.
- Manual product verification: `NOT APPLICABLE` because no product behavior exists to verify.
- Documentation validation: PASS. All internal document references resolve; decision identifiers are unique through `DECISION-024`; no document still records `ORG-Q2` as open; superseded decisions retain their annotations; historical labels remain intact on all six superseded product documents.
- Git operation status at this entry: branch created and the approved commit made. **Nothing was pushed, no pull request was opened, and nothing was merged.** No dependency was installed, no contract deployed, and no onchain transaction executed.
- Review status: The independent review of this planning checkpoint has **not** been performed. Under the mandatory lifecycle it is required before merge, not before commit.
- Remaining gates: independent review; `SPEC-001` approval and commit; `TASK-001` creation; `ORG-Q1` and `ORG-Q3` remain open with the organisers and neither blocks implementation.
- Rollback point: `3a4f106830d17e14051bdc38970e5b6ab9a048b1` on `master`.
- Known-good checkpoint: `master` at `3a4f106` remains the last known-good state until this branch is reviewed and merged.

## 2026-09-03: DECISION-025 Two-Tool Operating Model Addendum

- Scope: Documentation-only process clarification, prepared on the existing `docs/base-agent-authority-pivot` branch.
- Decision reference: `DECISION-025`.
- Trigger: Arko supplied a role-and-action table for the `SPEC-001` implementation phase and asked for review before adoption.
- Review performed before any file changed: compared the table against `AGENT_BUILD_INSTRUCTIONS.md` Sections 7 (Review Protocol) and 8 (Commit Protocol). Found the table consistent in substance, but surfaced three items needing explicit resolution rather than silent adoption: (1) Section 8's "agents do not commit directly" is ambiguous between a literal human-keystroke reading and an approval-gated-execution reading; (2) the current text is silent on who pushes a branch or opens a pull request; (3) the table implicitly staffs the ten-role fleet in Section 4 with two AI tools (Claude, Codex) and Arko rather than ten distinct agents.
- Arko's resolution: Confirmed the table; directed that it be recorded formally.
- Files changed: `AGENT_BUILD_INSTRUCTIONS.md` (new Section 11 plus a one-line cross-reference from Section 4), `DECISIONS.md` (`DECISION-025`), `HUMAN_DECISIONS.md`, `AI_USAGE.md`, this entry.
- Application implementation: None.
- Automated implementation tests: `NOT APPLICABLE` because no application code, executable schema, dependency, or scaffold changed.
- Manual product verification: `NOT APPLICABLE` because no product behavior changed.
- Documentation validation: PASS. `DECISION-025` is unique and sequential; the new Section 11 does not renumber or contradict Sections 1–10; the Section 4 cross-reference resolves; no approval boundary was widened for any agent.
- Git operation status: Nothing staged, committed, pushed, or merged as of this entry. Per the operating model this entry documents, the pending commit requires Arko's explicit approval before Claude executes it.
- Review status at this checkpoint: Independent Codex review requested next; see the following entry for its result and the corrections made.
- Rollback point: `d82f224` (the prior commit on this branch).

## 2026-09-03: First Independent Codex Review Of DECISION-025, And Corrections

- Review status: `NOT READY FOR COMMIT`.
- Reviewer: Codex, given a self-contained prompt drafted by Claude per Section 11 of `AGENT_BUILD_INSTRUCTIONS.md` itself (this addendum applying its own newly-recorded process to its own review, before commit).
- Review scope: Exactly the five uncommitted files on top of `d82f224` — `AGENT_BUILD_INSTRUCTIONS.md`, `DECISIONS.md`, `HUMAN_DECISIONS.md`, `AI_USAGE.md`, `BUILD_LOG.md`.
- Blocker 1: The table's compressed treatment of `SAVE PROMPT` and `UPDATE AUDIT DOCUMENTS` ("folded into… alongside 'Write implementation'" / "alongside 'Explain changed files'") did not preserve the Mandatory Lifecycle's required ordering in `AI_BUILD_GOVERNANCE.md`, specifically risking audit documents being updated before manual-verification and fix-verification results existed to record. The same compression was repeated in `DECISIONS.md`.
- Blocker 2: `AI_USAGE.md` asserted the operating-table prompt was not saved under `prompts/` because it was "short" — an unauthorized, unilateral exception to the prompt-preservation requirement, made by Claude without asking. Codex correctly identified this as the same category of overreach already corrected once in this same conversation (Claude deciding a governance step did not apply, instead of applying it).
- Important finding 1: The claim that the session's model switched from Opus 5 to Sonnet 5 mid-session at Arko's direction was asserted in `AI_USAGE.md` with no corroborating repository record, so an independent reviewer working only from repository state could not verify it.
- Important finding 2: `AGENT_BUILD_INSTRUCTIONS.md` Section 11 named Codex as the owner of Section 7 item 4 but did not name any owner for Section 7 items 2 and 3 (the Orchestrator and Product Spec Agent checks), leaving them able to disappear silently once the ten-role fleet collapsed to two tools.
- Passed checks, stated by the reviewer without prompting: `d82f224` is the correct base and current `HEAD`; review scope was exactly the five stated files; `DECISION-025` is sequential after `DECISION-024` with no duplicate; the Section 4 cross-reference to Section 11 resolves; the Mandatory Commit Gate heading remains intact after the new section; no product, authority, permission, or implementation behavior changed; and push, PR creation, and merge remain exclusively Arko's in the wording reviewed.
- Corrections applied:
  1. `AGENT_BUILD_INSTRUCTIONS.md` Section 11: replaced the compressed ordering note with an explicit statement that `SAVE PROMPT` precedes "Write implementation" and `UPDATE AUDIT DOCUMENTS` follows "Manually test product" and "Verify fixes" and precedes "Approve commit." Added explicit ownership of Section 7 items 2 and 3 to Claude's bullet, and clarified Codex owns item 4 only.
  2. `DECISIONS.md` `DECISION-025`: matched the corrected ordering language and the corrected item-2/3 ownership; added the first-pass Codex review as context; added a reference to the now-saved prompt file.
  3. Created `prompts/2026-09-03-two-tool-operating-model.md`, transcribing Arko's table verbatim, and disclosing in the file itself that it was written as a correction rather than contemporaneously with the original exchange.
  4. `AI_USAGE.md`: removed the "not saved because short" claim and pointed to the real saved-prompt file; qualified the model-switch claim to point at this entry as a retrospective, unverified self-report rather than asserting it as contemporaneous or independently verified.
- Model-switch record, retrospective and unverified: mid-session, Arko ran `/model claude-sonnet-5` to switch this session from Opus 5 to Sonnet 5; the switch preserved full conversation context; all work from that point in the session forward, including this correction, was performed under Sonnet 5. This entry was written during this corrective pass, after the switch occurred, not at the time of the switch itself — it is a same-session self-report, not contemporaneous corroboration, and no independent record confirms it.
- Application implementation: None.
- Automated implementation tests: `NOT APPLICABLE` because no application code, executable schema, dependency, or scaffold changed.
- Manual product verification: `NOT APPLICABLE` because no product behavior changed.
- Git operation status: No file was staged or committed, and nothing was pushed or merged during these corrections.
- Review status after corrections: A second independent Codex pass has not yet been run. Per the operating model this addendum itself records, that pass is expected before Arko is asked to approve the commit.
- Rollback point: `d82f224` (the prior commit on this branch); no commit exists yet for this addendum.

## 2026-09-03: Second Independent Codex Review Of DECISION-025, Fix, And Arko's Review-Pass Cap

- Review status: `NOT READY FOR COMMIT`.
- Reviewer: Codex, second independent pass on the same pending change, prompted per the standing rule to draft the review automatically without being asked.
- Finding, IMPORTANT: `AI_USAGE.md` and the new saved prompt described a `BUILD_LOG.md` entry written during this same corrective session as "contemporaneous corroboration" of the Opus5-to-Sonnet5 model switch. Codex correctly identified this as an overclaim: the entry was written during a retrospective correction pass, not at the time of the switch, so it is a same-session self-report, not contemporaneous or independent corroboration.
- Confirmed clean by this pass: the `SAVE PROMPT` / `UPDATE AUDIT DOCUMENTS` ordering fix; the existence and honesty of `prompts/2026-09-03-two-tool-operating-model.md`; and the explicit Claude/Codex ownership split for Section 7 items 2 through 4. Also noted, correctly, as pre-existing and out of scope: a broken reference in `TASKS.md` (`PREREQ-002_TRACEABILITY_REVIEW.md` missing its `docs/product/` path prefix), already present in committed `d82f224` and not part of this diff.
- Correction applied: reworded the model-switch claim in all three locations (`AI_USAGE.md`, `BUILD_LOG.md`'s prior entry, and the saved prompt) from "contemporaneous corroboration" to "retrospective, unverified self-report," stating explicitly that the record was written after the switch during a later corrective pass and is not independently verified.
- What happened next: Claude drafted a third independent-review prompt for Codex per the standing default of drafting review prompts automatically and unasked. Arko stopped this before it was run and gave a direct instruction: cap independent review passes at two per turn, and record that cap in governance now.
- Correction applied for the cap: added a "Review Pass Cap" subsection to `AGENT_BUILD_INSTRUCTIONS.md` Section 11, and folded the cap into `DECISION-025`'s Decision and Consequences text. The third review prompt Claude had drafted was withdrawn and was never sent to Codex or executed.
- Current state of the pending change as of this entry: two independent Codex passes have run and their findings addressed; the fix for pass 2's finding has **not** been independently re-verified by a third pass, per the newly-set cap. This is disclosed rather than presented as independently confirmed.
- Application implementation: None.
- Automated implementation tests: `NOT APPLICABLE` because no application code, executable schema, dependency, or scaffold changed.
- Manual product verification: `NOT APPLICABLE` because no product behavior changed.
- Git operation status: No file was staged or committed, and nothing was pushed or merged during these corrections.
- Review status after this entry: Two of two permitted independent passes complete. Whether to approve the commit with the unverified pass-2 fix, direct a further specific check, or treat it as a follow-up is Arko's decision per the cap rule just recorded.
- Rollback point: `d82f224` (the prior commit on this branch); no commit exists yet for this addendum.

## 2026-09-03: Submission-Practice Note Added To HACKATHON_RULES.md

- Scope: Documentation-only. Added a "Submission Practice: AI-Tool Attribution" section to `HACKATHON_RULES.md`, explicitly marked as self-imposed and not a verified official Sibyl Labs rule, recording that both Claude and Codex will be attributed visibly in the PR description and README, not only in the internal audit trail.
- Trigger: Arko asked whether AI-tool attribution survives Arko personally running `git push` instead of Claude. Answered: yes, attribution lives in commit trailers and the audit trail regardless of who pushes. Arko then asked for this practice to be recorded in `HACKATHON_RULES.md`.
- Independent review: **Explicitly waived by Arko for this change** ("no need for codex"). Per `AGENT_BUILD_INSTRUCTIONS.md` Section 7's own escape hatch — a review requirement may be waived by Arko with the rationale recorded — this is recorded rather than silently skipped. Rationale: single-file documentation addition, no product/authority/approval-boundary content, explicitly framed as not an official rule to avoid the exact failure mode independent review would normally catch (inventing an event rule).
- Files changed: `HACKATHON_RULES.md`.
- Application implementation: None.
- Automated implementation tests: `NOT APPLICABLE` because no application code, executable schema, dependency, or scaffold changed.
- Manual product verification: `NOT APPLICABLE` because no product behavior changed.
- Git operation status: Committed as a new, separate commit on `docs/base-agent-authority-pivot` per Arko's explicit approval to commit. Not pushed. Arko will push this commit together with the prior `fab9cff` commit at a later time.
- Rollback point: `fab9cff` (the prior commit on this branch).

## 2026-09-03: TASKS.md Reconciled To Current State

- Scope: Documentation-only status correction. `TASKS.md` had gone stale — it was last touched as part of the `d82f224` pivot commit and was never updated when `DECISION-025` (two-tool operating model), the AI-attribution submission-practice note, or the PR #3 merge into `master` happened.
- Trigger: Arko asked what needs to happen next; the answer surfaced that `TASKS.md`'s "Immediate Build Sequence" table still showed "Independent review of this planning checkpoint: Not started" and "Commit the planning checkpoint: Not started" despite the commit having happened. Arko decided not to run a retroactive Codex review against the original `d82f224` pivot content (step 4 in that table) and asked only for the status table to be corrected.
- Corrections made:
  - "Implementation Readiness" header now states `DECISION-022` through `DECISION-025` are approved and merged via PR #3.
  - Build-sequence step 4 marked `WAIVED 2026-09-03 by Arko`, with an explanatory note that `d82f224`'s content predates the two-tool review model and was not retroactively reviewed, distinct from `DECISION-025`'s own addendum (two Codex passes) and the attribution note (one explicitly-waived pass).
  - Build-sequence step 5 marked `DONE`, naming all three commits and the PR #3 merge.
  - `PREREQ-003` status corrected from "AWAITING ARKO'S APPROVAL" to "COMPLETE / APPROVED AND COMMITTED" — it was approved (build-sequence step 2) and committed, but the prerequisite's own status block hadn't been updated to say so.
  - "Unresolved Dependencies" gained a line recording `DECISION-025` as the resolved dependency governing `SPEC-001`'s eventual implementation.
- What did not change: the actual remaining gate. `SPEC-001` is still `PROPOSED`, not approved or committed; `TASK-001` remains blocked on it; nothing here authorizes implementation.
- Independent review: Not requested for this change; a plain status-accuracy correction with no product, authority, or process-rule content.
- Files changed: `TASKS.md`, `BUILD_LOG.md`, `AI_USAGE.md`.
- Application implementation: None.
- Automated implementation tests: `NOT APPLICABLE` because no application code, executable schema, dependency, or scaffold changed.
- Manual product verification: `NOT APPLICABLE` because no product behavior changed.
- Git operation status: Committed and merged into `master` via PR #4.
- Rollback point: `53a5d5e` (current `master` at the time, includes the direct README edit).

## 2026-09-03: SPEC-001 Review, Correction, And Approval

- Scope: Fixed docs/specs/SPEC-001_FRESH_SESSION_LEARNED_AUTHORITY_SLICE.md and its governing documents for approval, then Arko approved SPEC-001 itself.
- Trigger: Arko asked to move to SPEC-001. Before presenting it, Claude found SPEC-001 described its own governing decision (DECISION-023) as still "Proposed," when it had actually been approved and committed on 2026-09-03 — the same category of unpropagated status fix already made for TASKS.md earlier this session. Fixed that first, then presented SPEC-001 for review with an automatically-drafted Codex prompt per the standing default.
- Independent review, pass 1: NOT READY FOR COMMIT. 3 BLOCKERs, 2 IMPORTANT, 1 NICE-TO-HAVE:
  - BLOCKER: DECISION-023's canonical status in DECISIONS.md, PREREQ-003_ARCHITECTURE.md, and PRD.md still said Proposed/pending, contradicted by SPEC-001's own claim of "approved." Root-caused: Arko's earlier approval ("approval given use MIT license") was never propagated into these canonical status fields. Fixed at the source across all three files, plus ARCHITECTURE.md and README.md, which had the same problem.
  - BLOCKER: SPEC-001 section 13's file-ownership restriction literally forbade the mandatory prompts/, AI_USAGE.md, HUMAN_DECISIONS.md, BUILD_LOG.md, REUSED_COMPONENTS.md updates required by AGENT_BUILD_INSTRUCTIONS.md. Fixed with an explicit carve-out.
  - BLOCKER: the active corpus did not faithfully instantiate the retained PREREQ-002 model — PrecedentRelationship records referenced a matter version (MV-002-V1) instead of a decision version, and the required initial "No prior state -> draft" authority event was missing from both SPEC-001 and ACTIVE_DEMO_DESIGN.md's Session 1 walkthrough. Fixed by giving CASE-002 a resulting decision version DV-002-V1 created at write-back, and splitting the single "draft -> active" step into two distinct, correctly-attributed events (Decision Reviewer confirmation, then separate Authority Steward activation) in both documents.
  - IMPORTANT: the directional risk-tier comparability rule contradicted its own CASE-005 fixture (a high-risk precedent is actually comparable to a low-risk current proposal under the stated rule, not excluded). Fixed by redefining CASE-005 around a target_class mismatch instead, and adding a note clarifying the risk-tier direction is proven by test_comparability.py rather than a corpus fixture, since every corpus proposal is fixed at low risk.
  - IMPORTANT: acceptance criterion A14 (repeatable reset) had no mapped test. Fixed by mapping it to test_fresh_session.py's setup phase.
  - NICE-TO-HAVE: the VERIFY-AT-BUILD fallback note overstated its own coverage. Fixed to state it covers R3 (authority-event storage) only.
- Independent review, pass 2 (final, per the two-pass cap): NOT READY FOR COMMIT. 2 BLOCKERs:
  - BLOCKER: TASKS.md — cut from master before the earlier reconciliation branch existed — still had the same DECISION-023 status conflict in three places (PREREQ-003 block, SPEC-001 summary, TASK-001 Creation Gate condition 5). Fixed directly on this branch.
  - BLOCKER: the fix for the first BLOCKER's relationship-persistence gap didn't go far enough — persisting a PrecedentRelationship is specifically an Authority Steward confirmation per the retained PREREQ-002 contract (requiring fact_ids and citation_ids), distinct from the Decision Reviewer confirmation that creates the draft decision version. Fixed by making relationship persistence its own explicit, separately-timestamped Authority Steward step in both SPEC-001 and ACTIVE_DEMO_DESIGN.md, without activating DV-002-V1.
- Cap reached: two independent Codex passes ran against this change; both sets of findings were fixed and verified against source. Per AGENT_BUILD_INSTRUCTIONS.md section 11, a third pass required Arko's explicit direction rather than continuing automatically.
- Arko's decision: "approve as-is." Read as approving both the accumulated fixes and SPEC-001 itself, given the turn's framing was explicitly about moving SPEC-001 toward approval. Claude propagated the resulting "SPEC-001 approved" fact across SPEC-001 itself, TASKS.md (Implementation Readiness, Immediate Build Sequence, Unresolved Dependencies, TASK-001 Creation Gate conditions 2 and 8 and the gate-status summary, and the Specifications block), README.md, and ARCHITECTURE.md — applying the same grep-sweep discipline that two Codex passes had just validated, without a third review round, since this propagation is mechanical application of an already-reviewed pattern rather than new substantive content.
- Result: TASK-001 Creation Gate is now 9 of 9 satisfied. TASK-001 may be created.
- Files changed: ARCHITECTURE.md, DECISIONS.md, PRD.md, README.md, TASKS.md, docs/architecture/PREREQ-003_ARCHITECTURE.md, docs/product/ACTIVE_DEMO_DESIGN.md, docs/specs/SPEC-001_FRESH_SESSION_LEARNED_AUTHORITY_SLICE.md.
- Application implementation: None. This entire cycle was documentation and specification correction; no code exists yet.
- Automated implementation tests: `NOT APPLICABLE` — no code to test.
- Manual product verification: `NOT APPLICABLE` — no product behavior exists yet.
- Git operation status: Committed as `cd6172c` on `docs/spec-001-status-accuracy`. Merging into `master` required resolving a conflict against PR #4's independent `TASKS.md` reconciliation (see the merge-resolution note below); both branches had touched `TASKS.md`, `AI_USAGE.md`, and `BUILD_LOG.md` in overlapping ways because `docs/spec-001-status-accuracy` was branched before PR #4 merged.
- Rollback point: `53a5d5e` (branch point before PR #4).

## 2026-09-03: Merge Conflict Resolved Between PR #4 And PR #5

- Cause: `docs/spec-001-status-accuracy` (this branch) was created from `master` at `53a5d5e`, before `docs/reconcile-tasks-status` (PR #4) merged. Both branches independently corrected overlapping `DECISION-023`/`SPEC-001` status language in `TASKS.md`, and both appended new entries to `AI_USAGE.md` and `BUILD_LOG.md`. GitHub could not auto-merge PR #5 as a result.
- Resolution approach: `git merge origin/master` locally (not a rebase, so `cd6172c` and PR #4's commits both keep their original hashes; no force-push needed).
- `TASKS.md`: both sides described the same facts at different points in time. Kept the more detailed step 4/5 wording from PR #4 (the `WAIVED` note, exact commit hashes) and combined it with this branch's later truth that `SPEC-001` is now approved and `TASK-001` is unblocked, rather than picking one side wholesale.
- `AI_USAGE.md` and `BUILD_LOG.md`: both sides had appended a genuinely different, non-contradictory entry after the same base point — not a real disagreement, just two branches growing the log independently. Kept both entries, ordered by when each activity actually happened (PR #4's `TASKS.md` reconciliation, 12:40 UTC, before this branch's SPEC-001 approval work).
- Application implementation: None.
- Automated implementation tests: `NOT APPLICABLE`.
- Manual product verification: `NOT APPLICABLE`.
- Git operation status: Merge resolved locally, not yet committed as the merge commit.
- Rollback point: `cd6172c` (this branch before the merge).

## 2026-09-03: TASK-001 Defined; Seam (a) Implemented — Models, Policy, Authority Engine

- Scope: First application code in this repository. `TASK-001` drafted in `TASKS.md` as the umbrella authorization for the full `SPEC-001` implementation; seam (a) — "models, policy, and the pure authority engine with its invariant tests" — implemented per `SPEC-001` section 14's build order.
- Governing task/spec: `TASK-001`, `SPEC-001`.
- Files created: `pyproject.toml`; `finne/__init__.py`, `finne/models.py`, `finne/policy.py`, `finne/authority/__init__.py`, `finne/authority/comparability.py`, `finne/authority/derivation.py`, `finne/authority/engine.py`, `finne/memory/__init__.py`, `finne/base/__init__.py` (empty placeholders for seams b/d); `config/owner_policy.toml`; `tests/test_authority_invariants.py`, `tests/test_authority_states.py`, `tests/test_comparability.py`.
- Files modified: `TASKS.md` (`TASK-001` added), `.gitignore` (`*.egg-info/`, `.pytest_cache/`, `.hypothesis/` added).
- Implementation-level decisions made and recorded (routine, reversible, per the velocity rule — not escalated as blockers):
  - **Python version:** `DECISION-023` specified Python 3.11; neither 3.11 nor 3.12 is installed on this machine. Used 3.13.1 (the more conservative of the two available interpreters, 3.13.1 and 3.14.3), which satisfies `sibyl-memory-client`'s `>=3.10` requirement. Recorded in `prompts/2026-09-03-task-001-and-seam-a-kickoff.md`.
  - **`HardPolicy` schema:** No document defines `HardPolicy`'s exact fields — `PRD.md`'s five-input formula names it conceptually; `PREREQ-003` defers physical serialization to implementation. Implemented as a single optional `max_amount_override`, defaulting to a no-op, since no V1 scenario requires an active temporary override. The engine clamps any override above the owner ceiling down to the ceiling, so a malformed or malicious override can never widen authority.
  - **`config/owner_policy.toml`'s `approved_target_classes`:** Added `yield_vault_aggressive` alongside the two classes named in `ACTIVE_DEMO_DESIGN.md` section 1, because `CASE-005` (added during the `SPEC-001` review cycle) is a legitimately-authorized historical case under that target class — it is excluded from Session 2's derivation by comparability (target-class mismatch), not by being outside the owner's approved scope. Documented inline in the TOML file.
- What the authority engine does: `derive_effective_authority()` computes the strict intersection of the owner ceiling, hard policy, active/comparable/successful precedent eligibility, the learned constraint, and the current proposal's scope. Pure function — no I/O, no clock, no network, no imports from `finne.memory` or `finne.base`, matching `PREREQ-003`'s module-boundary requirement.
- Tests: 17 tests, all passing. Includes `hypothesis` property tests proving invariants 1 (owner ceiling never exceeded), 2 (no scope widening), 3 (hard-policy override can only narrow, including deliberately-adversarial overrides above the owner ceiling), and 5 (a retrieval miss can only narrow authority) — run at both the default example count and a stress pass at 2,000 examples per test (12,000 total), zero failures. Also reproduces the full `CASE-001`..`CASE-008` corpus from `ACTIVE_DEMO_DESIGN.md` directly against the engine and confirms the documented "25,000 proposed -> 10,000 authorized, citing `DV-001-V1`" outcome holds at the authority-logic level, before retrieval or Sibyl Memory exist.
- Automated implementation tests: PASS — `pytest tests/ -q` → 17 passed.
- Manual product verification: Not yet performed by Arko; this is seam (a) of five, no end-to-end demo exists yet to manually verify.
- Independent review: Not yet requested. Per the standing default, a Codex review prompt is being prepared for this change before commit.
- Application implementation: The authority engine, comparability rule, derivation policy, and data models — the deterministic core the whole product's authority claim rests on. No memory integration, no retrieval, no Base adapter, no CLI yet.
- Git operation status: Not yet committed. Branch `feature/seam-a-authority-engine`, created from `master`.
- Rollback point: `master` at the commit before this branch.

## 2026-09-03: First Independent Codex Review Of Seam (a), And Corrections

- Review status: `NOT READY FOR COMMIT`.
- Reviewer: Codex, first independent pass against the first application code in this repository.
- Blocker 1: `finne/models.py` validated only comparisons, never the runtime type or finiteness of monetary values — `OwnerPolicy`, `Proposal`, and `EvaluatedCandidate` all silently accepted `float`, including `float('nan')`, which passes a naive `< 0` check without raising. Confirmed: an `AuthorizationDecision.authorized_amount` could end up as a literal Python `float` in the ALLOW branch, where `min()` returns the first argument unchanged when it's already the smaller value. Fixed with a `_require_finite_decimal` check (`isinstance(..., Decimal)` and `.is_finite()`) applied to every monetary field via the shared `_require_positive_or_zero` helper, plus `tests/test_models_validation.py` — new adversarial tests proving float, NaN, Infinity, string, and `None` are all rejected at every monetary boundary.
- Blocker 2: `finne/authority/engine.py` checked scope before the owner amount ceiling, so an out-of-scope proposal above 25,000 USDC returned `escalate`, not the `block` NEG-04 requires "regardless of any precedent." Fixed by reordering: the amount-ceiling check now runs first, so BLOCK — the absolute, non-owner-resolvable violation — always takes priority over ESCALATE — the softer, owner-resolvable one. Added `test_out_of_scope_and_over_ceiling_blocks_not_escalates` to prove the interaction explicitly; split the original scope-only test to isolate that case with an in-ceiling amount.
- Blocker 3: `config/owner_policy.toml` added `yield_vault_aggressive` to the owner's approved target classes, widening the owner permission ceiling beyond what `ACTIVE_DEMO_DESIGN.md` section 1 actually confirms (`demo_receipt`, `yield_vault_conservative` only). On inspection, this widening was unnecessary: `finne.authority.comparability.compare()` never consults `owner_policy.approved_target_classes` at all — it excludes `CASE-005` by direct fact matching against the current proposal, not by an owner-policy scope check. Reverted to exactly the two confirmed classes, in the TOML and in both test files' hardcoded `OwnerPolicy` fixtures, which carried the same unnecessary widening independently.
- Important finding 1: the zero-authority ESCALATE branch always attributed the cause to `learned_constraint` and always stated "no precedent supports this," regardless of the actual cause. A real, eligible, active precedent combined with a `HardPolicy` override of zero would still report "no precedent," misattributing the true cause. Fixed by computing the tightest binding ceiling once, before branching on ESCALATE vs. CONSTRAIN, so both cases correctly attribute to whichever of `learned_constraint`, `current_hard_policy`, or `owner_permission_ceiling` actually drove the result — and still cite the eligible precedent for context even when it isn't the binding reason. Added `test_zero_ceiling_from_hard_policy_blames_hard_policy_not_precedent` and a contrasting `test_zero_ceiling_from_true_cold_start_blames_learned_constraint`.
- Important finding 2: `pyproject.toml` declared `requires-python = ">=3.10"`, but `finne/policy.py` imports `tomllib`, which is stdlib-only from Python 3.11 onward — an internal inconsistency that would fail immediately on a real 3.10 install. Fixed by bumping the declared floor to `>=3.11`, matching both `tomllib`'s actual requirement and `DECISION-023`'s original target; this does not conflict with running 3.13.1 locally, which satisfies `>=3.11`.
- Noted, not a finding: Codex observed that bare `pytest tests/ -q` fails collection without an editable install or `PYTHONPATH` first. This matches the documented run procedure in `PREREQ-003` section 15 (`pip install -e ".[dev]"` before `pytest`), not a gap.
- Corrections applied and reverified: full suite re-run at default and at 2,000-examples-per-hypothesis-test — 59 tests, all passing (up from 17; new tests added for every finding).
- Application implementation: None beyond the fixes described above; no new product behavior, only correctness fixes to the already-described seam (a).
- Automated implementation tests: PASS — 59 passed, 0 failed, both at default and stress example counts.
- Manual product verification: Not yet performed by Arko.
- Git operation status: No file was staged or committed, and nothing was pushed or merged during these corrections.
- Review status after corrections: One of two permitted independent passes complete. A second pass is being requested per the standing default before Arko is asked to approve the commit.
- Rollback point: `master` at the commit before this branch.

## 2026-09-03: Second Independent Codex Review Of Seam (a), And Final Corrections

- Review status: `NOT READY FOR COMMIT`.
- Reviewer: Codex, second and — per the two-pass cap — final independent pass against seam (a).
- Blocker: `finne/policy.py`'s `load_owner_policy()` called `Decimal()` directly on TOML values. An unquoted TOML number (e.g. `max_amount = 0.1` without quotes) parses as a native Python `float`; `Decimal(float)` does not reject that, it silently expands the float's exact binary value into a long, imprecise Decimal — verified empirically: `Decimal(0.1)` == `Decimal('0.1000000000000000055511151231257827021181583404541015625')`. This bypassed the Decimal-only boundary added in the first review round at its actual source, before any dataclass validation ran. Fixed with `_decimal_from_toml_string()`, which requires the TOML value to already be a string and raises `ValidationError` otherwise. Added `tests/test_policy.py` (new) with a direct regression test loading an unquoted-number TOML file and confirming rejection, plus confirmation the real `config/owner_policy.toml` still loads correctly.
- Nice-to-have: `tests/test_models_validation.py`'s docstring claimed coverage "at every boundary" without directly testing `OwnerPolicy.cold_start_autonomous_amount` or `AuthorizationDecision.authorized_amount`. Both already passed through the shared validator correctly — not a behavior defect — but added direct test cases for both to make the claim and the regression coverage accurate.
- Confirmed clean by this pass: the BLOCK-before-ESCALATE reordering (adversarial cases traced by hand: out-of-scope-and-over-ceiling blocks; in-scope-ceiling-with-wrong-network still escalates); the hard-policy-zero attribution fix (correctly distinguishes real-precedent-zeroed-by-override from true cold start); the `yield_vault_aggressive` removal (confirmed absent from all three locations, `CASE-005`'s own candidate facts correctly left alone, golden-path corpus test still produces 10000/`DV-001-V1`); the `>=3.11` Python floor.
- Cap reached: two independent Codex passes have now run against seam (a). Per `AGENT_BUILD_INSTRUCTIONS.md` section 11, a third pass requires Arko's explicit direction.
- Corrections applied and reverified: full suite re-run at default and 2,000-examples-per-hypothesis-test — 78 tests, all passing (up from 59).
- Application implementation: None beyond the fixes described; no new product behavior.
- Automated implementation tests: PASS — 78 passed, 0 failed.
- Manual product verification: Not yet performed by Arko.
- Git operation status: No file was staged or committed, and nothing was pushed or merged during these corrections.
- Rollback point: `master` at the commit before this branch.

## 2026-09-04: Seam (b) — Sibyl Memory Adapter, VERIFY-AT-BUILD Resolved

- Scope: Second of five SPEC-001 build seams. Implements the Sibyl Memory adapter — the module that makes memory genuinely load-bearing, per DECISION-022 and the hackathon's mandatory gate.
- Governing task/spec: TASK-001, SPEC-001 section 14 seam (b).
- VERIFY-AT-BUILD performed first, as SPEC-001 and PREREQ-003 both required: installed sibyl-memory-client==0.8.0 and empirically verified its real API against a live local SQLite database (constructed clients, wrote and read entities/references/state/events, tested overwrite and not-found behavior, tested tenant isolation) rather than relying on the published README, which documents an older surface.
- Material findings, all confirmed by direct exercise:
  - No `sibyl init` credentials required for local operations — resolves HACKATHON_RULES.md ORG-Q3.
  - `set_entity` does not enforce immutability; confirms the adapter-level write-once design is necessary.
  - `get_entity` raises `NotFoundError` for missing records; `get_reference`/`get_state` return `None` instead.
  - `write_event`'s real parameters (`evaluated`, `acted`, `forward`, `extra`, `ts`) have no generic payload slot; authority events are tagged via `extra["kind"]`.
  - `read_events(limit=...)` is descending-order and content-unfilterable, so `client.search(query, tiers=("journal",))` was used for R3 instead — reliable, no fallback needed, unlike the `AE-<sequence>` fallback PREREQ-003 had proposed.
  - `get_reference` returns dict bodies JSON-stringified under a `"body"` key, unlike `get_entity`. Found via a failing round-trip test (`read_owner_policy_snapshot` initially returned `None` instead of matching the written value) — fixed by parsing the string before reconstruction.
- Documentation corrected to match verified reality: HACKATHON_RULES.md (ORG-Q3 resolved), PREREQ-003_ARCHITECTURE.md (three spots — the original VERIFY-AT-BUILD note, the fallback note, and the open-items list), SPEC-001 (VERIFY-AT-BUILD line), ARCHITECTURE.md (open questions), TASKS.md (two lines). Treated as fact-correction, consistent with the earlier DECISION-023 status-propagation fixes, not a new architecture decision — the underlying design (immutability enforced above the store, authority state derived by folding) is unchanged; only the exact mechanism and the credential claim were corrected.
- Files created: `finne/memory/schema.py` (storage-shaped records: CaseVersionRecord, OutcomeRecord, OwnerPolicySnapshot, AuthorityEventRecord, with JSON (de)serialization and validation), `finne/memory/client.py` (MemoryStore — the only module importing sibyl_memory_client), `tests/test_memory_roundtrip.py` (21 tests against real temporary Sibyl Memory databases, not mocks).
- Files modified: `finne/models.py` (validation helpers made public — `require_finite_decimal`, `require_positive_or_zero`, `require_nonempty` — for reuse in schema.py; mechanical rename, re-verified against the full seam (a) suite, no behavior change).
- Process note: this work was initially done directly against a checked-out `master` rather than a feature branch — caught and corrected before any commit by creating `feature/seam-b-memory-adapter` and carrying the uncommitted work onto it.
- Tests: 21 new tests, all passing on first run against real (not mocked) temporary Sibyl Memory databases. Full suite: 99 passed (78 from seam (a) plus 21 new), including hypothesis property tests.
- Automated implementation tests: PASS — `pytest tests/ -v` → 99 passed.
- Manual product verification: Not yet performed by Arko.
- Independent review: Not yet requested. Per the standing default, a Codex review prompt is being prepared before commit.
- Application implementation: The memory adapter, its storage schema, and round-trip tests. finne/authority/ is not yet wired to finne/memory/ — that assembly is seam (c), retrieval.
- Git operation status: Not yet committed. Branch `feature/seam-b-memory-adapter`, created from `master` at the seam (a) merge commit.
- Rollback point: `master` at the commit before this branch.

## 2026-09-04: Second Independent Codex Review Of Seam (b), And Final Corrections

- Review status: `NOT READY FOR COMMIT`.
- Reviewer: Codex, second and — per the two-pass cap — final independent pass. Ran live code against the installed sibyl-memory-client package rather than reading claims and trusting them.
- Blocker 1: `_authority_events_for` requested `limit=1000` from `client.search(..., tiers=("journal",))`, but the installed client silently caps real results at `limit // 4` regardless of how many actually match — confirmed by Codex with a live 1,001-event write, and independently reconfirmed here across limit=20/100/400/1000/100000 (ratio held consistently, no pagination parameter exists). This directly contradicted the "reliably retrieve every authority event... regardless of total event count" claim recorded after the first review. Fixed: requests `limit=8000` (effective cap 2000, far beyond any realistic corpus here), and treats hitting that cap as a truncation signal — `fold_authority_state` fails safe to `None` rather than trusting a possibly-incomplete history, since FTS5 relevance ordering means truncation could drop any subset, not necessarily the oldest or newest.
- Blocker 2: `fold_authority_state` validated nothing about transition legality — it simply took the chronologically-last event's `new_status`. Live-confirmed by Codex: `No prior state -> active` and `withdrawn -> active` both folded successfully as `active`, violating PREREQ-002's Authority Transitions table and terminal-state enforcement outright. Fixed at two layers: `AuthorityEventRecord.__post_init__` now rejects any (previous_status, new_status) pair not in the new `LEGAL_TRANSITIONS` matrix (making illegal single events unconstructable, whether from live code or deserialized via `from_extra`), and `fold_authority_state` additionally verifies cross-event chain consistency — each event's own claimed `previous_status` must match what the sequence has actually accumulated so far, or folding stops at the last valid prefix.
- Blocker 3: `_authority_events_for` called `.get()` on `body`/`extra` without checking they were dicts first; a non-mapping payload (a list, which `write_event`'s own `extra: Any` signature permits) crashed with `AttributeError` instead of being excluded. Fixed with explicit `isinstance(..., dict)` guards before any `.get()` call.
- Blocker 4: `from_body`/`from_extra` accepted a missing `schema_version` (silently defaulting to current) and any present value (e.g. 999) without checking it matched. Fixed with a new `_require_current_schema_version` helper, applied to all four record types, that rejects both missing and mismatched versions — and, found while implementing the fix, also guards the non-dict-body case that would otherwise crash the same way as Blocker 3 (`.get()` on a list raises `AttributeError`, uncaught by any reader's except clause).
- Important finding 1: `PREREQ-003_ARCHITECTURE.md`'s W1-W5/R1-R5 table still named `read_events(...)` for R3 after the first review's prose-only correction. Fixed the table row, and corrected the accompanying prose, which had also overclaimed "regardless of total event count" — now states the real, verified cap and the truncation-detection design.
- Important finding 2: the check-then-write immutability pattern (`get_entity`/`get_reference` then `set_entity`/`set_reference`) is not atomic against the underlying store, which exposes no compare-and-set primitive. Resolved per Codex's own stated acceptable resolution ("atomic, or explicitly constrain to a serialized writer"): added a `threading.Lock` around all three write paths, closing the race for concurrent callers within one process, and documented explicitly in the module and class docstrings that cross-process atomicity is not provided and is not needed — this project's architecture is single-writer-at-a-time by design (Session 1 fully exits before Session 2 starts).
- Corrections applied and reverified: every fix confirmed by direct re-execution against the live installed package, matching Codex's exact repro conditions, not just by reading the new code. Full suite re-run at default and 2,000-examples-per-hypothesis-test: 110 tests, all passing (up from 99; 11 new regression tests, one per finding plus the terminal-state matrix check).
- Cap reached: two independent Codex passes have now run against seam (b). Per `AGENT_BUILD_INSTRUCTIONS.md` section 11, a third pass requires Arko's explicit direction.
- Application implementation: None beyond the fixes described; no new product behavior, only correctness fixes to already-described seam (b) behavior.
- Automated implementation tests: PASS — 110 passed, 0 failed.
- Manual product verification: Not yet performed by Arko.
- Git operation status: No file was staged or committed, and nothing was pushed or merged during these corrections.
- Rollback point: `master` at the commit before this branch (the seam (a) merge, `1076bfe`).

## 2026-09-04: Seam (c) — Retrieval And The Two Session Scripts

- Scope: Third of five `SPEC-001` build seams. Connects `finne/authority/` (seam a) and `finne/memory/` (seam b) for the first time — the point at which the two-session demo becomes runnable end to end for the authority-and-memory claim.
- Governing task/spec: `TASK-001`, `SPEC-001` section 14 seam (c). Kickoff prompt: `prompts/2026-09-04-seam-c-retrieval-sessions-kickoff.md`.
- `finne/retrieval.py`: `find_candidates(proposal, memory, *, limit=50)` builds a deliberately coarse query (network/asset/action_class only — `_QUERY_DIMENSIONS`) so material-difference fixtures are retrieved rather than filtered out at the search stage; `comparability.compare()` (seam a) does the real filtering. Excludes candidates with no folded authority state or no recorded outcome, since those cannot yet be judged eligible either way. Retrieval-miss-only-narrows proven as a Hypothesis safety property.
- `finne/base/adapter.py`: minimal, explicitly-labeled stub matching the interface `PREREQ-003` section 17 already specified (`record_authorization`, `get_receipt`), not a functional Base integration — that is seam (d)'s scope. `BaseExecutionResult.attempted` distinguishes "seam (d) not built yet" from "genuinely attempted and failed" (NEG-07), so session scripts can report a real, non-fabricated `SUCCESS` outcome now (`tx_hash=None`) while still correctly failing when a future real attempt genuinely fails.
- `finne/demo_config.py`: the fixed demo tenant ID and the shared facts (network/asset/action_class/target_class/function) `scripts/reset_demo.py`, `session1.py`, and `session2.py` all build proposals from.
- `scripts/reset_demo.py`: clears the demo tenant's case-version/outcome entities and re-seeds `CASE-003` through `CASE-008` exactly per `ACTIVE_DEMO_DESIGN.md` section 5 (withdrawn, active, material-difference, superseded, questioned, and never-activated draft fixtures). `CASE-001`/`CASE-002` are deliberately never seeded — they are created live by the session scripts, so Session 2's recall is genuine rather than staged.
- `scripts/session1.py`: proposes 25,000 USDC against the 25,000 ceiling with no comparable active precedent, expects `escalate`, accepts the owner's constrained 10,000 USDC approval (10,000 is `ACTIVE_DEMO_DESIGN.md`'s documented demo value, encoded as the script's default so `test_fresh_session.py` can run non-interactively; `--owner-approved-amount` overrides it for a live recording), calls the Base stub, and persists the complete case (case version, owner-policy snapshot, two authority events — Decision Reviewer then Authority Steward — and the outcome) before exiting completely.
- `scripts/session2.py`: a genuinely separate process. Proposes the same 25,000 USDC, retrieves `DV-001-V1` (now active) among the seeded corpus, and expects `constrain` to 10,000 USDC, printing the change and naming `DV-001-V1` as the binding precedent on screen (A5). `--no-memory` points at a fresh, never-seeded tenant instead of touching the real demo tenant, reproducing the organiser's deletion test (A6, invariant 6) without destroying `DV-001-V1`. Persists `DV-002-V1` at `draft` only — activation and `PrecedentRelationship` object persistence are out of scope for this slice, matching the same scope boundary already recorded in `reset_demo.py`'s docstring.
- `tests/test_fresh_session.py`: runs the scripts as real, separate `subprocess.run()` calls, not as in-process function imports — proving A3 (no carried-over in-process state) requires an actual OS-level process boundary, which a Python import cannot demonstrate. Covers A1, A3, A4, A5, A6, A14, invariant 6.
- Real bug found via this test, not by inspection: `MemoryStore.clear_all_case_data_for_demo_reset()` only hard-deletes `finne_case_version`/`finne_outcome` entities; it cannot clear `DV-001-V1`'s owner-policy-snapshot reference or authority-event journal entries, because `sibyl-memory-client` exposes no delete for either (references are only overwritable, not enumerable or deletable; the journal is append-only by design). A second `reset_demo.py` + `session1.py` run against the same database file therefore hit the write-once `IntegrityError` on the owner-policy snapshot. This is not a bug in the adapter — write-once immutability for those two record types is invariant 8, working as intended. Fixed by scoping `test_demo_resets_and_rehearses_repeatably` to start each full rehearsal from a fresh database file (deleting it first), matching how a real re-take of the demo would actually reset — `reset_demo.py`'s own clear is correctly scoped to the reseedable `CASE-003`..`008` fixtures only, not to a live-created case's permanent history.
- Files created: `finne/retrieval.py`, `finne/base/adapter.py`, `finne/demo_config.py`, `scripts/reset_demo.py`, `scripts/session1.py`, `scripts/session2.py`, `tests/test_retrieval.py`, `tests/test_fresh_session.py`, `prompts/2026-09-04-seam-c-retrieval-sessions-kickoff.md`.
- Files modified: `finne/memory/client.py` (`clear_all_case_data_for_demo_reset()` added, so `reset_demo.py` does not need to reach into the private `_client` attribute — preserves the module boundary that `finne.memory.client` is the only code talking to `sibyl_memory_client`).
- Tests: 14 new tests (5 in `test_fresh_session.py`, 9 in `test_retrieval.py`), all passing on first run against real (not mocked) temporary Sibyl Memory databases and real subprocess invocations. Full suite: 124 passed (110 from seams a+b, plus 14 new).
- Automated implementation tests: PASS — `pytest tests/ -v` → 124 passed.
- Manual product verification: Smoke-tested end to end by Claude against a real temporary database outside the automated suite: `reset_demo.py` → `session1.py` (escalates, owner approves 10,000, case persisted) → `session2.py` (retrieves `DV-001-V1`, constrains 25,000 to 10,000, cites it by name) → `session2.py --no-memory` (retrieves nothing, escalates). Not yet performed by Arko.
- Independent review: Not yet requested. Per the standing default, a Codex review prompt is being prepared before commit.
- Application implementation: Retrieval, the Base adapter stub, and both session scripts. The two-session demo is now runnable end to end for the authority-and-memory claim (not yet for the Base-execution claim, which is seam (d)).
- Git operation status: Not yet committed. Branch `feature/seam-c-retrieval-sessions`, created from `master` at the seam (b) merge commit (`5739358`).
- Rollback point: `master` at the commit before this branch.

## 2026-09-04: First Independent Codex Review Of Seam (c), And Corrections

- Review status: `NOT READY FOR COMMIT`.
- Reviewer: Codex, first of the two permitted independent passes. A second, unrelated Codex response was pasted into this conversation first, reviewing stale pre-seam-(b)-round-2 code (test count 99, findings already fixed) — identified as stale by comparing the current test count (124) and confirming every cited fix marker (`_JOURNAL_SEARCH_LIMIT`, `LEGAL_TRANSITIONS`, `_require_current_schema_version`, the write lock) was already present in the live tree, before Arko re-sent the correct seam (c) review.
- Blocker 1: `session1.py` and `session2.py` submitted the engine's own raw `escalate`/`0` decision to `record_authorization`, then unconditionally wrote `Outcome.SUCCESS` regardless of whether Base was attempted. Re-reading `PREREQ-003` section 3's W1-W4 table settled this precisely: W1 (case), W2 (owner-policy snapshot), and W3 (authority events) are written "after the owner constrains authority" / "on owner confirmation," independent of Base — but W4 (the outcome) is written only "after the Base transaction settles." Fixed in both scripts: W1-W3 now persist immediately; the owner's manual override after an escalation is now represented as an explicit, distinct `AuthorizationDecision` (constructed via `dataclasses.replace`, not by `finne.authority.engine`, which stays pure and owner-override-unaware) rather than the stale zero-amount escalate decision; W4 is written only when `attempted=True`, with `Outcome.FAILURE` on a genuine attempted failure and no outcome at all while the stub remains unattempted. Any decision authorizing zero (`escalate`/`block`) with no owner override now stops the script entirely — nothing submitted to Base, nothing persisted for that case.
  - Consequence, verified by re-running the full flow: `session1.py` now correctly leaves `DV-001-V1` with no recorded outcome while seam (d) does not exist, so `finne.authority.derivation` correctly treats it as ineligible (invariant 4) and `session2.py` now honestly escalates too, rather than fabricating the `constrain` result the smoke test previously reported. **A4/A5 cannot be demonstrated live by `session1.py -> session2.py` alone until seam (d) exists** — this was previously masked by the outcome-timing bug fixed above. `docs/specs/SPEC-001_FRESH_SESSION_LEARNED_AUTHORITY_SLICE.md` section 12 corrected accordingly (fact-correction, not a new architecture decision — the design was already right; the table's test-file assignment was wrong): A4/A5 moved to `test_base_adapter.py`, alongside A11/A12. `test_fresh_session.py` now proves the retrieval/derivation/relationship-persistence logic underlying A4/A5 directly, by seeding a precedent's outcome the same way `reset_demo.py` already seeds `CASE-003`..`008` (a synthetic-but-honest stand-in for a real seam (d) result), rather than claiming to prove the full live claim it cannot yet produce.
- Blocker 2: `session2.py` wrote `DV-002-V1` without the required W2 owner-policy snapshot, and never persisted the `follows` (`DV-002-V1` -> `DV-001-V1`) / `distinguishes` (`DV-002-V1` -> `DV-005-V1`) `PrecedentRelationship` records `ACTIVE_DEMO_DESIGN.md` section 7 step 11 explicitly requires — an unauthorized scope-narrowing on Claude's part that conflated "persisting the relationships" with "activating `DV-002-V1`" (only the latter is actually out of scope, per the same step's own text). Fixed: added `PrecedentRelationshipRecord`/`RelationshipType` to `finne/memory/schema.py` (append-only, per `PREREQ-002`'s retained contract — `relationship_id` implicit via journal identity, `fact_ids`/`citation_ids` required non-empty, populated with the comparability dimensions actually checked and the decision version(s) actually cited, since this slice has no separate Fact/Citation entity subsystem), `write_precedent_relationship`/`read_precedent_relationships_from` on `MemoryStore` (same journal-search-with-truncation-detection pattern as `_authority_events_for`), and `session2.py` now writes the owner-policy snapshot and both relationships immediately alongside W1/W3, before attempting Base — relationships depend on `DV-002-V1` existing, per step 11's own "now that DV-002-V1 exists" condition, not on its later outcome.
- Blocker 3: `MemoryStore.clear_all_case_data_for_demo_reset()` cannot clear `DV-001-V1`'s owner-policy-snapshot reference or authority-event journal entries (already found and disclosed in the previous entry), and Codex correctly pushed back that scoping the *test* around this (starting from a fresh file) masks the *product* still describing a same-tenant reset as working. Fixed at the product level: `reset_demo.py` now runs a pre-flight check (`read_owner_policy_snapshot` for `DV-001-V1`/`DV-002-V1`) and exits 1 with an actionable message — pointing `--db-path` at a new file — instead of leaving a confusing `IntegrityError` to surface later inside `session1.py`. New regression test `test_reset_refuses_when_prior_session_data_exists`.
- Important: `BaseExecutionResult` could represent `attempted=True, success=True, tx_hash=None` — an incoherent combination both call sites would have persisted as a success. Fixed with a `__post_init__` on the dataclass enforcing the only two coherent shapes (unattempted: no success, no hash; attempted-success: a real non-empty hash required) at the type level, so no caller can construct or receive an incoherent result at all.
- Nice-to-have: `clear_all_case_data_for_demo_reset` performed a permanent hard-delete without confirming which tenant it was about to wipe. Fixed: now requires `confirm_tenant_id`, verified against the store's actual tenant (via `MemoryClient.get_tenant()`) before proceeding, raising `ValueError` on a mismatch.
- Corrections applied and reverified: full flow re-run manually end to end confirming the new, honest behavior (escalate, persist W1-W3, no outcome; second session also escalates; `--no-memory` control escalates; a repeat `reset_demo.py` against the same file now fails with the actionable message instead of crashing later). `tests/test_fresh_session.py` substantially rewritten to test the corrected behavior plus a new seeded-outcome test proving `session2.py`'s constrain/cite/relationship logic directly. Full suite: 126 passed (up from 124; net +2 after removing the now-false "constrains live" assertions and adding the seeded-outcome and reset-residue tests).
- Cap status: one of two permitted independent passes complete for seam (c).
- Application implementation: `finne/base/adapter.py` (result-contract validation), `finne/memory/schema.py` (`PrecedentRelationshipRecord`, `RelationshipType`), `finne/memory/client.py` (relationship read/write, tenant-guarded reset), `scripts/reset_demo.py` (pre-flight residue check), `scripts/session1.py` and `scripts/session2.py` (corrected W1-W4 timing, explicit owner-override decision, relationship persistence, stop-on-non-authorizing-decision).
- Automated implementation tests: PASS — 126 passed, 0 failed.
- Manual product verification: Re-verified by Claude after the fixes (see above). Not yet performed by Arko.
- Git operation status: No file was staged or committed.
- Rollback point: `master` at the commit before this branch (the seam (b) merge, `5739358`).

## 2026-09-04: Second Independent Codex Review Of Seam (c)

- Review status: `NOT READY FOR COMMIT`.
- Reviewer: Codex, second and — per the two-pass cap — final permitted pass on seam (c).
- Blocker 1: `PrecedentRelationshipRecord`'s `fact_ids`/`citation_ids` (added in round 1) were field names and bare decision-version-id strings, not the `FactID`/validated `CitationID` objects `PREREQ-002`'s own contract requires; `relationship_id` was also absent entirely. Checked `PREREQ-002`'s full table: `CitationEdge` is its own entity (`citation_id`, `from_type`/`from_id`, `target_type`/`target_id`, `claim`, `created_by`, `created_at`, `validated_at`) with a rejection-audit path (`CitationAttemptAuditEvent`) for anything that fails validation — a genuine validated-reference subsystem, not a shape a few string tuples can satisfy. Codex offered two resolutions: implement the real subsystem, or defer relationship persistence from this seam. Given this capability is required by none of `SPEC-001`'s fourteen acceptance criteria, is not part of `PREREQ-003` section 3's load-bearing W1-W5/R1-R5 set, and building it properly under time pressure risks more bugs for a non-load-bearing feature, chose to defer: removed `PrecedentRelationshipRecord`/`RelationshipType`/`PRECEDENT_RELATIONSHIP_KIND` from `finne/memory/schema.py`, `write_precedent_relationship`/`read_precedent_relationships_from` from `finne/memory/client.py`, and the relationship-writing block from `session2.py`. `ACTIVE_DEMO_DESIGN.md` sections 6 and 7 step 11 corrected to state this deferral explicitly, with rationale — not silently reverted.
- Blocker 2: (resolved by the same deferral) `session2.py` auto-recorded relationships as `confirmed_by="owner_as_authority_steward"` with a hardcoded historical timestamp — neither a genuine confirmation nor a truthful audit time. Moot once relationship persistence is deferred.
- Important 1: `BaseExecutionResult.__post_init__`'s checks used truthiness, not type/value validation — `attempted="yes", success="yes", tx_hash=123` passed silently (reproduced directly: `not "yes"` is `False`, `"yes" and not 123` is `False`, so neither branch raised). Fixed with explicit `isinstance`/`is True`/`is False` checks on all three fields.
- Important 2: `reset_demo.py`'s pre-flight residue check looked only at the owner-policy snapshot, missing a hypothetical interrupted run that reached W1/W3 but not W2. Strengthened to check case version, snapshot, and folded authority state — any of the three now counts as residue, independent of this script's own current write ordering.
- Important 3: `--owner-approved-amount 0` (or negative) was accepted by `session1.py` and activated as an `ACTIVE` precedent authorizing `0.00` — reproduced directly against a temporary database (confirmed: `DV-001-V1` reached `authority_state=ACTIVE` with `authorized_amount=Decimal('0')`). Fixed: amounts `<= 0` are now refused before any write.
- Important 4: `PREREQ-003` section 14's own test table still claimed `test_fresh_session.py` proves the live 25,000-to-10,000 constraint, conflicting with the round-1 correction to `SPEC-001` section 12. Reconciled — both documents now describe the same, corrected test ownership.
- Nice-to-have: the seeded-outcome test's synthetic `SUCCESS` outcome used `base_tx_hash=None` — a shape a real seam-(d) success (now enforced by `BaseExecutionResult.__post_init__`) can never produce. Fixed: uses a clearly-synthetic non-empty identifier instead. The direct relationship-filtering/truncation tests Codex also requested are moot given the deferral above.
- Corrections applied and reverified: `BaseExecutionResult`'s rejection re-confirmed directly; `--owner-approved-amount 0`'s refusal re-confirmed directly (previously reached `ACTIVE`, now exits 1 before any write); full end-to-end flow re-run (escalate, persist W1-W3, no premature outcome; second session escalates; `--no-memory` escalates; repeat `reset_demo.py` correctly refused with residue). Full suite: 126 passed (unchanged in count — round 2 was a net-zero of removals and additions to the existing seeded-outcome test).
- Cap reached: two independent Codex passes have now run against seam (c). Per `AGENT_BUILD_INSTRUCTIONS.md` section 11, a third pass requires Arko's explicit direction. Findings from this pass are believed fixed and self-verified above but have not been independently re-checked by a third pass.
- Application implementation: `finne/base/adapter.py` (strict result validation), `finne/memory/schema.py` and `finne/memory/client.py` (relationship persistence removed), `scripts/reset_demo.py` (stronger residue check), `scripts/session1.py` (positive-amount validation), `scripts/session2.py` (relationship-writing removed), `tests/test_fresh_session.py`, `docs/product/ACTIVE_DEMO_DESIGN.md`, `docs/architecture/PREREQ-003_ARCHITECTURE.md`.
- Automated implementation tests: PASS — 126 passed, 0 failed.
- Manual product verification: Re-verified by Claude after the fixes (see above). Not yet performed by Arko.
- Git operation status: No file was staged or committed.
- Rollback point: `master` at the commit before this branch (the seam (b) merge, `5739358`).

## 2026-09-05: Seam (d) — Base Contract And Adapter

- Scope: Fourth of five `SPEC-001` build seams. Implements the real Base integration — the demo contract, the deploy script, and the `web3.py` adapter replacing seam (c)'s stub — making the Base-execution claim (A11, A12, A13, invariant 10) and the live version of A4/A5 genuinely demonstrable for the first time.
- Governing task/spec: `TASK-001`, `SPEC-001` section 14 seam (d).
- `finne/base/contracts/AuthorizationReceipt.sol`: matches `PREREQ-003` section 11's exact signature (`recordAuthorization(decisionId, authorizedAmount, factsHash)`, non-payable, emits `AuthorizationRecorded`); enforces onchain duplicate protection via `require(!recorded[decisionId])` (NEG-08); stores the full receipt in contract storage (`getReceipt(decisionId)` view function) rather than relying on event logs alone, matching the spec's own "verifiable evidence rather than a bare log line." Compiles cleanly with `solc 0.8.24`.
- `finne/base/env.py` (new): the only other reader of `FINNE_BASE_PRIVATE_KEY`/`BASE_RPC_URL` besides `finne/base/adapter.py` itself — `scripts/deploy_contract.py` imports from here rather than re-implementing its own `.env` parsing, keeping every reader of raw key material under `finne/base/` per `PREREQ-003` section 6. No `python-dotenv` dependency added; two `KEY=VALUE` lines don't need one.
- `scripts/deploy_contract.py` (new): compiles and deploys the contract via `py-solc-x`/`web3.py`, writes `config/base_deployment.json` (address, ABI, chain ID read from the connected RPC itself, not a separately configured value that could drift). Refuses to redeploy if the file already exists, unless `--force`.
- `finne/base/adapter.py` (real implementation, replacing the seam (c) stub): `record_authorization`'s signature gained a `proposal` parameter — the contract's `factsHash` field is specified as "a hash of the material facts and cited precedents," and the material facts (network/asset/action_class/target_class/function/counterparty_risk_tier/amount) live on `Proposal`, not `AuthorizationDecision`, so a real hash needs both; `scripts/session1.py`/`scripts/session2.py` updated to pass it, and their hardcoded "pending seam (d)" messaging removed now that a genuine reason (refused, connection failure, NEG-08 duplicate) is always available via `detail`. `decisionId = keccak256(decision_version_id)`; `factsHash` over a canonical sorted-key JSON payload; amounts converted to 6-decimal USDC integer units. Checks the contract's own `recorded()` state before submitting, refusing a doomed duplicate application-side (NEG-08) in addition to the contract's own guard.
- Real bugs found only by actually running this against live Base Sepolia infrastructure, not by inspection or mocked tests alone:
  - The fee calculation in the first draft of `deploy_contract.py` hardcoded a 1 gwei priority fee (reasonable on mainnet); Base Sepolia's actual gas price is a small fraction of a gwei, so the hardcoded priority fee exceeded the max fee and every submission was rejected outright (`max priority fee per gas higher than max fee per gas`). Fixed by deriving both fee components from the live network (`w3.eth.max_priority_fee`, `get_block("latest")["baseFeePerGas"]`) instead of an assumed constant — the same fix reused in `finne/base/adapter.py`'s own transaction building.
  - Running the automated test suite once, before `FINNE_BASE_DRY_RUN` existed, fired two genuine, permanent transactions at the live deployed contract for the fixed `DV-001-V1`/`DV-002-V1` decision_version_id values `scripts/session1.py`/`scripts/session2.py` use, because `tests/test_fresh_session.py`'s subprocess invocations now reach the real adapter the same way a live demo run does. Fixed with `FINNE_BASE_DRY_RUN=1`, set by `test_fresh_session.py`'s `run_script()` helper, skipping all network activity — matching `PREREQ-003` section 14's mocked-by-default, opt-in-live testing philosophy already specified for `test_base_adapter.py`, extended here to the subprocess tests that now indirectly exercise the same real code. Consequence: `DV-001-V1`/`DV-002-V1` are permanently used up on that first contract deployment; a fresh deployment (`--force`) is required before any further live rehearsal, exactly as a fresh database file already was for the local-only part of one.
  - `get_receipt()`'s first draft reconstructed the transaction hash via a single `eth_getLogs` call spanning the whole range from the deployment block to `"latest"`. Only hours after deployment, with just ~15,630 blocks elapsed, the public RPC endpoint rejected this outright (`413 Payload Too Large`) — a much smaller range than assumed safe, and silently swallowed by the function's own broad exception handling until traced down by running the opt-in live test for real and finding `get_receipt` returned `None` immediately after a `record_authorization` call that had just succeeded. Fixed with a bounded, chunked backward scan (2,000 blocks per request, up to 25 chunks) instead of guessing a different single "safe" window size.
  - A live, freshly-confirmed transaction's own `recorded()` state briefly read as `False` on an immediately-following, separately-connected read (confirmed by direct diagnostic: `False` then `True` three seconds later) — a real characteristic of public RPC read-replica propagation, not a bug to paper over. Does not affect correctness here since `record_authorization`'s own returned result already carries the true outcome from the same connection that submitted it.
- `tests/test_base_adapter.py` (new, per `PREREQ-003` section 14): 27 mocked tests against lightweight fakes standing in for exactly the `web3.py` surface the adapter calls (not a general-purpose test double) — every `record_authorization`/`get_receipt` path (refused pre-flight, dry-run, connection failure, read failure, NEG-08 duplicate, success, revert, timeout, submission failure), `BaseExecutionResult`'s coherence contract, the pure `decisionId`/`factsHash`/amount-conversion helpers, and invariant 10 (transaction never carries value) — plus 2 opt-in live tests gated by `FINNE_LIVE_BASE_TEST=1`, both run for real and passing against the live deployed contract.
- Full live end-to-end verification, not just tests: redeployed a fresh contract, then ran `reset_demo.py` → `session1.py` (real) → `session2.py` (real) at the default database path with no dry-run flag. Session 1 escalated at cold start, owner approved 10,000, real Base transaction succeeded (`0x4f34f5e2cc704c6b671aa00018ff625b30407ff9fb681190c08af0064e561109`), complete case persisted. Session 2, a genuinely separate process, retrieved `DV-001-V1` as eligible, autonomously constrained 25,000 to 10,000 citing it by name, and its own real Base transaction succeeded (`0x268ac832588cd13a89e2a63ce45b2e92425a19e47866e5c2f99e7a67e59b3d14`). `get_receipt()` independently reconstructed both records with matching hashes. `session2.py --no-memory`, run against the same non-empty tenant holding a real active eligible `DV-001-V1`, still escalated — memory is genuinely load-bearing, not decorative. Total cost across deployment and two authorizations: ~0.0000038 ETH, all testnet.
- Files created: `finne/base/contracts/AuthorizationReceipt.sol`, `finne/base/env.py`, `scripts/deploy_contract.py`, `tests/test_base_adapter.py`, `.env.example`, `config/base_deployment.json` (generated by `deploy_contract.py`, checked in per `PREREQ-003` section 11).
- Files modified: `finne/base/adapter.py` (stub replaced with the real implementation), `scripts/session1.py` and `scripts/session2.py` (`proposal` argument added to `record_authorization` calls; stale "pending seam (d)" messaging removed), `tests/test_fresh_session.py` (`FINNE_BASE_DRY_RUN=1` added to `run_script()`'s subprocess environment; module docstring corrected).
- Tests: 29 new (27 mocked, 2 live), all passing, both live tests run for real against live Base Sepolia infrastructure. Full suite: 155 collected, 153 passed, 2 skipped by default (the live tests; both pass when run with `FINNE_LIVE_BASE_TEST=1`).
- Automated implementation tests: PASS — `pytest tests/ -v` → 153 passed, 2 skipped (default); 155 passed with `FINNE_LIVE_BASE_TEST=1`.
- Manual product verification: Full live end-to-end run performed by Claude (see above), including the `--no-memory` control. Not yet performed by Arko.
- Independent review: Not yet requested. Per the standing default, a Codex review prompt is being prepared before commit.
- Application implementation: The Base contract, deploy script, and real adapter. The two-session demo is now runnable end to end for BOTH the authority-and-memory claim and the Base-execution claim — no capability remains stubbed.
- Git operation status: Not yet committed. Branch `feature/seam-d-base-adapter`, created from `master` at the seam (c) merge commit (`785d167`) — corrected mid-session after an earlier, accidental branch from a stale local `master` pointer that was missing seam (c) entirely; fixed by fast-forwarding local `master` to `origin/master` and recreating the branch before any commit was made, so no seam (c) work was ever at risk.
- Rollback point: `master` at the commit before this branch (the seam (c) merge, `785d167`).

## 2026-09-05: First Independent Codex Review Of Seam (d), And Corrections

- Review status: `NOT READY FOR COMMIT`.
- Reviewer: Codex, first of the two permitted independent passes on seam (d).
- Blocker 1: `_authorized_amount_units` used `ROUND_HALF_UP` — an authorized amount with more than six decimal places (e.g. `Decimal("10000.0000006")`) rounded UP to onchain units representing `10000.000001`, a higher amount than was actually authorized. Reproduced directly: confirmed the exact rounding-up before fixing. No current caller produces such an amount (both session scripts always deal in 2-decimal-place USDC), but this codebase's own standing rule — authorized_amount can never widen, including for adversarial or malformed input — makes "no current caller happens to trigger it" an insufficient defense. Fixed: raises `ValueError` when the amount isn't exactly representable in six decimal places, rather than rounding; `record_authorization` catches this as a clean pre-flight refusal (`attempted=False`), consistent with its other refusal paths, rather than letting an exception escape uncaught.
- Blocker 2: a receipt-wait timeout or RPC error after a successful broadcast was recorded by both session scripts as immutable `Outcome.FAILURE`, identically to a confirmed onchain revert — but a timeout is NOT a confirmed failure, it's an unknown, still-pending result; the transaction might yet be mined and succeed. Since `OutcomeRecord` is write-once, this could permanently misrepresent a genuinely successful precedent as failed, with no way to correct it later. Fixed by adding a third field to `BaseExecutionResult`, `outcome_confirmed: bool`, distinguishing a KNOWN result (success, an onchain revert, or an outright pre-broadcast rejection) from a genuinely unknown one (broadcast accepted, confirmation timed out/errored) — `__post_init__` now enforces that `success=True` can never coexist with `outcome_confirmed=False`. `scripts/session1.py`/`scripts/session2.py` now only write `Outcome.FAILURE` when `outcome_confirmed` is also `True`; an unconfirmed timeout writes no outcome at all, printing that `get_receipt(decision_version_id)` — which checks the contract's own state directly — is how to reconcile it later, rather than building a redundant separate reconciliation mechanism.
- Blocker 3: `scripts/deploy_contract.py` read the private key, constructed a signing account, and broadcast transactions directly — outside `finne/base/`, violating the module boundary this repository's own architecture (`PREREQ-003` section 6) and `finne/base/adapter.py`'s own docstring both state ("only `finne/base/adapter.py` holds key material or reaches the network"). This was a real tension in the approved planning docs that a prior turn's judgment call resolved the wrong way (treating a one-time deploy script as an implicit exception the boundary rule doesn't actually carve out). Fixed by moving all compile/deploy/sign/send logic into a new `finne.base.adapter.deploy_contract()` function; `scripts/deploy_contract.py` is now a thin CLI wrapper with no direct `eth_account`/`web3` imports. Verified by redeploying a fresh contract through the refactored path and re-running the full live demo end to end. Also added `tests/test_import_boundaries.py` (new, per `PREREQ-003`'s own test file list, previously unwritten) — AST-based static checks that no file outside `finne/base/` imports `eth_account`/`web3` and no file outside `finne/memory/client.py` imports `sibyl_memory_client`, so a future regression of this exact kind fails a test automatically rather than requiring a human or a Codex pass to notice.
- Important 1: `tests/test_base_adapter.py`'s fake event-log query ignored its own `from_block`/`to_block`/`argument_filters` arguments, so the mocked tests never actually exercised `_find_transaction_hash`'s chunked backward-scan logic beyond "found on the first chunk" or "never found." Fixed: the fake is now genuinely range-aware (an optional `log_block` parameter only returns the log when the queried range actually contains it), with new tests placing a log in the first, a middle, and the final (deployment-block-boundary) chunk, plus cap-exhaustion and mid-scan-RPC-failure cases.
- Important 2: `finne/base/env.py`'s docstring (and this review's own round-1 prompt, item 7) claimed the private key "never appears in any persisted file" — not true, since it necessarily lives in the local, gitignored `.env` file for this to work without an interactive prompt every run. Corrected the docstring to the accurate, narrower claim: never written to Sibyl Memory, logged, or committed to version control — not "never persisted anywhere." The round-1 prompt file itself is left as the contemporaneous record of what was actually asked, per this project's standing rule against rewriting history; the correction lives here and in the code, not by editing that file.
- Corrections applied and reverified: the rounding bug reproduced directly (`10000.0000006` → confirmed inflated units) then confirmed fixed (raises, doesn't round); the timeout/failure distinction re-run against all three new coherence tests; the module-boundary fix verified by actually redeploying a fresh contract through `deploy_contract()` and re-running the full live `reset_demo.py` → `session1.py` → `session2.py` → `session2.py --no-memory` sequence end to end, all real, all passing, including the `--no-memory` control against a real active precedent. The two live tests in `test_base_adapter.py` also needed a fix during this same pass: a real, previously-undiagnosed transient RPC read-replica lag (a fresh connection's read of just-confirmed state can briefly be stale) made both flaky under the stricter re-verification; added a short bounded retry to the live tests' own verification reads only — `record_authorization`'s returned result was never affected, since it's authoritative from the same connection that submitted it.
- Cap status: one of two permitted independent passes complete for seam (d).
- Application implementation: `finne/base/adapter.py` (amount validation, outcome-confirmation distinction, deploy_contract() added), `finne/base/env.py` (corrected persistence claim), `scripts/deploy_contract.py` (reduced to a thin wrapper), `scripts/session1.py` and `scripts/session2.py` (outcome_confirmed handling), `tests/test_base_adapter.py` (coherence tests, chunk tests, live-test retry tolerance), `tests/test_import_boundaries.py` (new).
- Automated implementation tests: PASS — 165 passed, 2 skipped by default; both live tests pass with `FINNE_LIVE_BASE_TEST=1` (re-run twice for reliability after the retry fix).
- Manual product verification: full live end-to-end re-run by Claude after all fixes (see above). Not yet performed by Arko.
- Git operation status: No file was staged or committed.
- Rollback point: `master` at the commit before this branch (the seam (c) merge, `785d167`).

## 2026-09-05: Third Independent Codex Review Of Seam (d) (Cap Extended By Arko)

- Review status: `NOT READY FOR COMMIT`.
- Reviewer: Codex, third pass on seam (d), per `HUMAN_DECISIONS.md`'s "Seam (d) Review Extended Beyond The Two-Pass Cap" — a comprehensive, fresh sweep of the whole seam rather than a check against specific prior findings.
- Blocker 1: `AuthorizationReceipt.sol`'s `recordAuthorization` had no access control at all — any address could call it. Since `decisionId = keccak256(decision_version_id)` and `decision_version_id` values ("DV-001-V1", "DV-002-V1") are fixed, public strings in this repository's own source, a third party could permanently record a predictable `decisionId` with arbitrary data, preempting a real authorization before this project's own session scripts ever run, or creating misleading "evidence." `get_receipt()` also never verified who actually submitted a given receipt. Reproduced directly: a simulated call from a freshly-generated, unrelated wallet succeeded against the then-current deployment. Fixed: the contract now has an `authorizedSigner` (set once, at deployment, to the deploying wallet) and `recordAuthorization` reverts for any other caller — verified by redeploying and simulating an attacker call, which now correctly reverts with `"AuthorizationReceipt: not the authorized signer"`. `get_receipt()` additionally verifies the stored receipt's `submittedBy` against the connected wallet's own address before trusting it, as defense in depth.
- Blocker 2: the `outcome_confirmed` fix (round 1) correctly stopped writing a false immutable failure on a receipt timeout, but nothing ever completed W4 for a pending case afterward — `get_receipt()` existed but nothing called it for this purpose, and re-running a session would collide with the now-immutable W1-W3. Fixed: added `finne.base.adapter.reconcile_pending(tx_hash)`, which checks the ORIGINAL transaction's own receipt directly (distinguishing "still pending" from "confirmed reverted" — both look identical from contract state alone, since a revert never writes anything) and a new `scripts/reconcile_outcome.py` that writes the resulting outcome to Sibyl Memory exactly once. `scripts/session1.py`/`scripts/session2.py`'s timeout messages now print the exact command to run later, including the real transaction hash, instead of a vague pointer to `get_receipt()`.
- Important 1: `BaseExecutionResult(attempted=True, success=False, outcome_confirmed=False, tx_hash=None, ...)` constructed without error, despite the documented requirement that an unconfirmed broadcast must carry its real hash. Reproduced directly, then fixed with an explicit `__post_init__` check; added the missing negative test.
- Important 2: `_connect()` never verified the live RPC's chain ID against `config/base_deployment.json`'s stored value — a misconfigured `BASE_RPC_URL` (wrong network, stale value after switching providers) could silently sign for an unintended chain. Fixed: fails closed on a mismatch. Also found: connection-error messages included the full RPC URL, which could leak an embedded API key if a paid provider (Alchemy/Infura/QuickNode — all named in `.env.example` as fallback options) were ever configured instead of the current public endpoint. Fixed with a redaction helper showing only the scheme and host.
- Important, governing-document drift: `PREREQ-003` still declared `record_authorization(decision)` (missing the `proposal` parameter added earlier in seam (d)) and said timeouts produce a recorded `failure` outcome, contradicting the `outcome_confirmed` fix; `SPEC-001` placed W4 before W3 in Session 1's steps and named a nonexistent `BaseAdapter` class; `SPEC-001`'s own note claiming `test_base_adapter.py` proves Session 2's live A4/A5 flow was itself unearned — that file's two live tests exercised the adapter's functions directly, never `session1.py`/`session2.py`. Traced the timeout/failure conflation to its root: `ACTIVE_DEMO_DESIGN.md`'s original, approved `NEG-07` row ("Base transaction reverts, times out, or fails → outcome recorded as failure") predates the write-once-immutability analysis that motivated `outcome_confirmed`, and both `PREREQ-003` and `SPEC-001` inherited it uncorrected. Fixed all three documents (fact-corrections, not new architecture decisions — the underlying safety property, established by round 1's fix, is more fundamental than the original wording); split `NEG-07` into `NEG-07` (confirmed revert/rejection) and a new `NEG-09` (broadcast accepted, confirmation times out — resolved later via reconciliation); corrected Session 1 and Session 2's step ordering in `ACTIVE_DEMO_DESIGN.md` to match the actually-implemented W1-W3-before-Base-before-W4 sequence. Rather than just re-documenting the A4/A5 test gap, closed it: added `test_live_session1_then_session2_constrains_citing_precedent` to `tests/test_base_adapter.py` — an opt-in live test that redeploys a fresh contract and runs `reset_demo.py` → `session1.py` → `session2.py` as real subprocesses with no seeding, asserting the live `constrain` result citing `DV-001-V1` with a real Base transaction. Run and passing.
- Corrections applied and reverified: the access-control fix verified by simulating an unauthorized call against the live redeployed contract (correctly reverts); the reconciliation workflow's core query (`reconcile_pending`) verified against `web3.exceptions.TransactionNotFound`'s real behavior before relying on it; full live end-to-end re-run one final time after every fix (fresh contract, `reset_demo.py` → `session1.py` → `session2.py` → `session2.py --no-memory`, all real, all passing).
- Cap status: three of up to four permitted passes complete for seam (d), per Arko's explicit extension.
- Application implementation: `finne/base/contracts/AuthorizationReceipt.sol` (access control added), `finne/base/adapter.py` (provenance check, chain-ID verification, URL redaction, `outcome_confirmed`/`tx_hash` coherence, `reconcile_pending` added), `scripts/session1.py`/`scripts/session2.py` (reconciliation command in timeout messages), `scripts/reconcile_outcome.py` (new), `tests/test_base_adapter.py` (new provenance/coherence tests, new full-live-flow test), `docs/product/ACTIVE_DEMO_DESIGN.md`, `docs/architecture/PREREQ-003_ARCHITECTURE.md`, `docs/specs/SPEC-001_FRESH_SESSION_LEARNED_AUTHORITY_SLICE.md`.
- Automated implementation tests: PASS — 167 passed, 3 skipped by default; all 3 live tests pass with `FINNE_LIVE_BASE_TEST=1`, including the new full-flow test.
- Manual product verification: full live end-to-end re-run by Claude after all fixes (see above), including the `--no-memory` control against the freshly-redeployed contract. Not yet performed by Arko.
- Git operation status: No file was staged or committed.
- Rollback point: `master` at the commit before this branch (the seam (c) merge, `785d167`).

## 2026-09-05: Fourth Independent Codex Review Of Seam (d) (Final, Per Arko's Extension)

- Review status: `NOT READY FOR COMMIT`.
- Reviewer: Codex, fourth and — per Arko's explicit "up to four rounds" extension — final pass on seam (d) unless a fifth is separately directed.
- Blocker: `reconcile_pending()` (added in round 3) took only a `tx_hash` and reported ANY successful receipt as proof of success for whatever `decision_version_id` the caller supplied — no check that the transaction targeted this contract, or that it actually authorized that exact `decisionId`. Reproduced directly against live data: a real, genuinely successful transaction for one decision, reconciled against a DIFFERENT `decision_version_id`, was accepted as that other decision's success before the fix. This is exactly the scenario the finding describes — an operator's copy-paste error (or a genuinely unrelated hash) could have persisted a fabricated successful outcome. Fixed: `reconcile_pending(decision_version_id, tx_hash)` now verifies the receipt's `to` matches the configured contract address, and — for a successful receipt — decodes its logs and requires an `AuthorizationRecorded` event whose `decisionId` matches `keccak256(decision_version_id)` before ever reporting success. Re-verified against real live data: the same real successful transaction now correctly refuses when reconciled against a different `decision_version_id`, citing "did not emit `AuthorizationRecorded`" rather than accepting it. Added 6 new mocked tests (unrelated transaction, mismatched decision ID, absent submission, confirmed revert, confirmed success) plus 2 new subprocess tests on `scripts/reconcile_outcome.py` itself (absent case version, repeated/idempotent reconciliation).
- Important: `deploy_contract()`'s connection-error message still included the raw, unredacted RPC URL — the redaction helper added earlier in seam (d) had been applied to `_connect()` but not to this separate connection path. Fixed.
- Nice-to-have: a malformed `tx_hash` (not valid hex) raised a `Web3ValueError` distinct from `TransactionNotFound`, which `reconcile_pending`'s original `except TransactionNotFound` did not catch — would have crashed `scripts/reconcile_outcome.py` with a traceback instead of a clean "unconfirmed" result. Confirmed the distinct exception type empirically before fixing with a broader catch that still correctly reports `outcome_confirmed=False` rather than fabricating any result.
- Verified without needing a fix, per Codex's own report: the `authorizedSigner` guard has no bypass path in the current (non-proxy, non-factory) deployment pattern; chain-ID validation runs before any account construction or transaction activity; `get_receipt()`'s existing provenance check is correctly bound to the locally-configured signer.
- Corrections applied and reverified: `reconcile_pending`'s new binding checks tested against real live data (correct match succeeds; the exact same real transaction reconciled against a wrong `decision_version_id` correctly refuses); full live end-to-end re-run one final time after every fix (fresh contract, `reset_demo.py` → `session1.py` → `session2.py` → `session2.py --no-memory`, all real, all passing).
- Cap status: four of the four rounds Arko authorized are now complete for seam (d). A fifth pass requires a further explicit direction, per `AGENT_BUILD_INSTRUCTIONS.md` section 11's underlying rule — not drafted automatically.
- Application implementation: `finne/base/adapter.py` (`reconcile_pending`'s binding verification, `deploy_contract`'s URL redaction), `scripts/reconcile_outcome.py` (updated call signature), `tests/test_base_adapter.py` (6 new tests), `tests/test_fresh_session.py` (2 new tests).
- Automated implementation tests: PASS — 175 passed, 3 skipped by default; all 3 live tests re-confirmed passing.
- Manual product verification: full live end-to-end re-run by Claude after all fixes (see above), plus direct live verification of `reconcile_pending`'s new checks against real transaction data. Not yet performed by Arko.
- Git operation status: No file was staged or committed.
- Rollback point: `master` at the commit before this branch (the seam (c) merge, `785d167`).

## 2026-09-05: Fifth Independent Codex Review Of Seam (d) (Abundant Caution, With Severity Triage)

- Review status: `NOT READY FOR COMMIT`. Reviewer: Codex, fifth pass, per `HUMAN_DECISIONS.md`'s "Seam (d) Review Extended To A Fifth Pass" — Arko also asked that findings be triaged as deal-breakers versus nice-to-haves. Codex reported 5 deal-breakers and 4 nice-to-haves; all 5 deal-breakers and 2 of the 4 nice-to-haves are fixed below, with the remaining 2 explicitly deferred and justified.
- Deal-breaker 1 — **round 4's own fix created a worse bug than the one it closed.** `reconcile_pending` returned `success=False, outcome_confirmed=True` for an unrelated or mismatched transaction; `scripts/reconcile_outcome.py` correctly read that as "confirmed execution failure" and persisted an immutable `Outcome.FAILURE`. Reproduced end to end: reconciling `DV-001-V1` against an unrelated transaction printed "refusing to treat it as evidence" and then immediately wrote that transaction as `failure` evidence, exiting `0`. Fixed by introducing `ReconciliationMismatch`, raised for anything that cannot be bound to the decision — a mismatch is an operator error about *which transaction to look at*, not a fact about the decision's outcome, and raising makes the two impossible to conflate. Re-verified: the same unrelated hash, and a malformed hash, now both refuse and persist nothing.
- Deal-breaker 2 — a reverted receipt was accepted as failure evidence based only on its `to` address. Because a reverted transaction emits no events, the round-4 event check never applied to the revert path, so any reverted transaction to this contract could be attached as immutable failure evidence for any decision. Fixed by binding from the transaction's own **calldata** (sender, recipient, function selector, and the exact `decisionId` argument) — which works identically for successful and reverted transactions. Verified live by deliberately broadcasting a real reverting duplicate submission: reconciling the correct decision against it returns "confirmed reverted"; reconciling a *different* decision against the same real reverted transaction correctly raises `ReconciliationMismatch`.
- Deal-breaker 3 — every `send_raw_transaction` exception was classified as a confirmed pre-broadcast rejection, but a transport-level failure can occur *after* the node accepted the transaction. This recreated the exact write-once false-failure hazard `outcome_confirmed` was introduced to close, just at the broadcast boundary instead of the receipt-wait boundary. Fixed by splitting `_build_and_send` into `_build_and_sign` plus an explicit send, computing the transaction hash locally from the signed transaction *before* broadcasting: build/estimate/sign failures remain confirmed failures (genuinely nothing pending), while an ambiguous send failure now returns `outcome_confirmed=False` with a real, reconcilable hash.
- Deal-breaker 4 — `SPEC-001` section 4's Session 2 still placed Base execution before W1-W3 and still required persisted `PrecedentRelationship` records, contradicting both the implementation and `ACTIVE_DEMO_DESIGN.md`, which had already been corrected. Round 3 fixed Session 1's ordering in this document but not Session 2's. Its test-mapping table also still claimed relationship-persistence coverage that no test provides and no code implements. All corrected.
- Deal-breaker 5 — `PREREQ-003` section 18's repository layout omitted `finne/base/env.py`, `scripts/reconcile_outcome.py`, and (from seam (c)) `finne/demo_config.py`, while listing `scripts/seed_demo.py`, which was never created. Since `SPEC-001` section 13 forbids application files outside that exact layout, three real files sat outside the specification's authorized implementation boundary. Corrected, along with section 9's stale "two operations" claim (now four: `record_authorization`, `get_receipt`, `reconcile_pending`, `deploy_contract`).
- Nice-to-have, fixed: `_redact_rpc_url` stripped the URL path but returned `netloc`, which includes user-info — an `https://user:key@host/` endpoint would still have leaked its credential. Now strips to scheme, hostname, and port only. Also fixed the module docstring in `tests/test_base_adapter.py` claiming one opt-in live test when there were three, and added `test_live_contract_rejects_an_unauthorized_signer` — a regression guard for round 3's most serious finding, simulating (not broadcasting, so zero gas) an unauthorized call and asserting it reverts.
- Nice-to-have, **deliberately deferred**: (1) `finne/base/env.py` does not enforce `.env` file permissions. The current `.env` is already `0600` (verified), this is a single-developer hackathon machine, and adding permission enforcement risks breaking the demo on a differently-configured machine days before the deadline for no gain in what the rubric actually evaluates. (2) `deploy_contract()` does not preserve a locally-computed deployment transaction hash if broadcasting becomes ambiguous, which could orphan a contract and waste testnet gas — it cannot widen authority or corrupt any record, because no deployment configuration is written unless the receipt confirms, and recovery is simply re-running the script.
- Corrections applied and reverified: deal-breakers 1 and 2 both reproduced *before* fixing and re-verified *after* against real live Base Sepolia data (including deliberately broadcasting a real reverting transaction to test the revert-binding path); full live end-to-end re-run one final time (fresh contract, `reset_demo.py` → `session1.py` → `session2.py` → `session2.py --no-memory`, all real, all passing).
- Cap status: five passes complete for seam (d), the fifth by Arko's further explicit extension. A sixth requires another explicit direction.
- Application implementation: `finne/base/adapter.py` (`ReconciliationMismatch`, calldata binding, local pre-broadcast hash, user-info redaction), `scripts/reconcile_outcome.py` (mismatch handling), `tests/test_base_adapter.py` (reconcile tests rewritten for the new contract, new ambiguous-broadcast and pre-broadcast-failure tests, new live signer-restriction guard), `docs/specs/SPEC-001_FRESH_SESSION_LEARNED_AUTHORITY_SLICE.md`, `docs/architecture/PREREQ-003_ARCHITECTURE.md`.
- Automated implementation tests: PASS — 179 passed, 4 skipped by default; all 4 live tests pass with `FINNE_LIVE_BASE_TEST=1`.
- Manual product verification: full live end-to-end re-run by Claude after all fixes (see above). Not yet performed by Arko.
- Git operation status: No file was staged or committed.
- Rollback point: `master` at the commit before this branch (the seam (c) merge, `785d167`).
